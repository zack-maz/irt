---
phase: 27.3-water-facility-filtering-improvements
verified: 2026-04-17T08:35:00Z
status: passed
score: 14/14 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 13/14
  gaps_closed:
    - 'server/__tests__/routes/water.test.ts passes without new regressions (G-01/WR-02 — emptyStats fixture, all 6 tests pass)'
    - 'ATTACK_EVENT_TYPES extracted to single shared constant WATER_ATTACK_EVENT_TYPES; all 3 consumers import it (WR-01 REV-5 consistency)'
  gaps_remaining: []
  regressions: []
---

# Phase 27.3: Water Facility Filtering Improvements — Verification Report

**Phase Goal:** Fix dam filtering (union tags + relaxed notability), reduce reservoir noise to 50-200 via HOLISTIC filter, remove treatment_plant type entirely, enrich facilities with capacity/population/river data via bbox-pre-filtered pipeline, preload facility data via dev file cache, fix the invisible-attacked-water-sites bug, and add dev filter diagnostics. Make the water layer Palantir-grade using only free/public APIs.
**Verified:** 2026-04-17T08:35:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure (Plans 27.3-03 closed G-01 and WR-01)

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                          | Status   | Evidence                                                                                                                                                                               |
| --- | ---------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Dam query returns waterway=dam UNION man_made=dam                                              | VERIFIED | `server/adapters/overpass-water.ts:266` — `(nwr["waterway"="dam"];nwr["man_made"="dam"];)`                                                                                             |
| 2   | treatment_plant fully removed from server types, queries, normalization                        | VERIFIED | `WaterFacilityType = 'dam' \| 'reservoir' \| 'desalination'` (server/types.ts:161); 0 matches in src/; only comment references in server/                                              |
| 3   | Reservoirs filtered via wikidata OR (named AND priority country)                               | VERIFIED | `normalizeWaterElement` line ~478: `isNotable(el.tags) \|\| (inPriority && hasName(el.tags))`                                                                                          |
| 4   | notabilityScore computed; facilities below MIN_NOTABILITY_SCORE=25 rejected                    | VERIFIED | `computeNotabilityScore` exported; `MIN_NOTABILITY_SCORE = 25` at line 448; cutoff applied in normalizeWaterElement                                                                    |
| 5   | RIVER_BBOXES pre-computed at module init; bbox pre-filter in linkRiver                         | VERIFIED | `export const RIVER_BBOXES` at line 235; bbox guard in `linkRiver` before vertex iteration                                                                                             |
| 6   | Dev file cache: saveDevWaterCache/loadDevWaterCache wired into water route                     | VERIFIED | `server/cache/devFileCache.ts:81,97`; route checks `loadDevWaterCache` before Overpass, calls `saveDevWaterCache` after cold fetch                                                     |
| 7   | fetchWaterFacilities returns {facilities, stats} with enrichment coverage + histogram          | VERIFIED | Return type `Promise<{facilities: WaterFacility[]; stats: WaterFilterStats}>` at line 574; 5-bucket histogram computed                                                                 |
| 8   | Spatial dedup (50m, same facilityType)                                                         | VERIFIED | Dedup loop in `fetchWaterFacilities` using haversine < 0.05 km                                                                                                                         |
| 9   | treatment_plant fully removed from all client-side types, toggles, icons, counters, labels     | VERIFIED | `grep -rn treatment_plant src/` = 0 matches; ALL_WATER_TYPES = ['dam','reservoir','desalination']                                                                                      |
| 10  | WaterFacilityDetail shows capacity, nearestCity, linkedRiver, notabilityScore when data exists | VERIFIED | `WaterFacilityDetail.tsx` lines 183-241: conditional sections for all 4 enrichment fields; notabilityScore gated on import.meta.env.DEV                                                |
| 11  | DevApiStatus shows Water Filters diagnostics                                                   | VERIFIED | `WaterFiltersSection` component defined and rendered at DevApiStatus.tsx:684,686,697; shows raw/filtered counts, rejections, enrichment, histogram                                     |
| 12  | waterStore holds filterStats; useWaterFetch forwards from API response                         | VERIFIED | `waterStore.ts:53` filterStats field + setFilterStats action; `useWaterFetch.ts:45-47` guards with !== undefined check                                                                 |
| 13  | Attacked water facilities are visible on the map (REV-5 bug fix)                               | VERIFIED | `src/lib/waterAttackEvents.ts:14-19` — `WATER_ATTACK_EVENT_TYPES = {airstrike, explosion, targeted, on_ground}`; imported by useWaterLayers.ts:167 and used in the destroyed-id filter |
| 14  | server/**tests**/routes/water.test.ts passes without new regressions                           | VERIFIED | `npx vitest run server/__tests__/routes/water.test.ts` — 6/6 pass. `emptyStats` fixture at line 20 satisfies `waterFilterStatsSchema` with all 5 required sub-fields                   |

**Score:** 14/14 truths verified

### Gap Closure Confirmation (Plan 27.3-03)

#### G-01 / WR-02 — CLOSED

- **Previous state:** `stats: {}` mock at lines 17, 161, 185 violated `waterFilterStatsSchema`; Zod rejected → 500 response → Test 1 asserted `body.stale === false` but received `true`.
- **Fix applied:** `emptyStats` constant defined at line 20 with all 5 sub-fields (`rawCounts`, `filteredCounts`, `rejections` with 5 keys, `enrichment` with 3 keys, `scoreHistogram` as typed array). All three `mockResolvedValue` call sites updated to use it. Mock return type narrowed from `stats: unknown` to `stats: typeof emptyStats`.
- **Verification:** `npx vitest run server/__tests__/routes/water.test.ts` → 6/6 pass (confirmed 2026-04-17T08:33Z)

#### WR-01 — CLOSED

- **Previous state:** `useWaterLayers.ts` declared a local `ATTACK_EVENT_TYPES = {airstrike, explosion, targeted, on_ground}` (REV-5 expanded). `WaterFacilityDetail.tsx:39` and `useCounterData.ts:285` still used a narrow `new Set(['airstrike', 'explosion'])` — three-way inconsistency on "attacked" definition.
- **Fix applied:** New file `src/lib/waterAttackEvents.ts` exports `WATER_ATTACK_EVENT_TYPES` (19 lines). Inline declarations removed from all three consumers. Each consumer imports `WATER_ATTACK_EVENT_TYPES` from `@/lib/waterAttackEvents`.
- **Verification:** `grep -rl "WATER_ATTACK_EVENT_TYPES" src/` → 4 files (definition + 3 consumers); `grep -rn "new Set(\['airstrike', 'explosion'\])" src/` → 0 matches; `grep -rn "const ATTACK_EVENT_TYPES\|const DESTRUCTIVE_EVENT_TYPES\|const COMBAT_EVENT_TYPES" src/` → 0 matches.

### Required Artifacts

| Artifact                                           | Expected                                              | Status   | Details                                                                                                                                                               |
| -------------------------------------------------- | ----------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `server/types.ts`                                  | WaterFacilityType 3 members + 4 enrichment fields     | VERIFIED | Line 161: `'dam' \| 'reservoir' \| 'desalination'`; lines 190-205: capacity, nearestCity, linkedRiver, notabilityScore                                                |
| `server/adapters/overpass-water.ts`                | Holistic filtering, enrichment, bbox, filter stats    | VERIFIED | All exports present: fetchWaterFacilities, classifyWaterType, WaterFilterStats, extractCapacityTags, findNearestCity, linkRiver, computeNotabilityScore, RIVER_BBOXES |
| `server/cache/devFileCache.ts`                     | saveDevWaterCache/loadDevWaterCache                   | VERIFIED | Lines 81, 97                                                                                                                                                          |
| `server/routes/water.ts`                           | Dev cache check + {facilities, stats} destructuring   | VERIFIED | loadDevWaterCache check before Overpass; filterStats in response envelope                                                                                             |
| `server/__tests__/adapters/overpass-water.test.ts` | 98 tests, all passing                                 | VERIFIED | All 98 pass (confirmed Plan 01 + 02 checks; unchanged by Plan 03)                                                                                                     |
| `server/__tests__/routes/water.test.ts`            | 6/6 tests passing, emptyStats fixture present         | VERIFIED | 6/6 pass; `const emptyStats` at line 20; no `stats: {}` literal in file                                                                                               |
| `src/lib/waterAttackEvents.ts`                     | Single WATER_ATTACK_EVENT_TYPES export                | VERIFIED | 19 lines; exports `Set<string>(['airstrike','explosion','targeted','on_ground'])`; JSDoc names all 3 consumer sites                                                   |
| `src/stores/filterStore.ts`                        | ALL_WATER_TYPES 3 members                             | VERIFIED | Line 9: `['dam', 'reservoir', 'desalination']`                                                                                                                        |
| `src/components/detail/WaterFacilityDetail.tsx`    | Enrichment sections + shared WATER_ATTACK_EVENT_TYPES | VERIFIED | Lines 183-241: capacity, nearestCity, linkedRiver, notabilityScore (dev-only); imports WATER_ATTACK_EVENT_TYPES from @/lib/waterAttackEvents                          |
| `src/components/ui/DevApiStatus.tsx`               | WaterFiltersSection                                   | VERIFIED | Lines 684-686: component defined and rendered                                                                                                                         |
| `src/stores/waterStore.ts`                         | filterStats state + setFilterStats                    | VERIFIED | Lines 53, 64, 101                                                                                                                                                     |
| `src/hooks/useWaterFetch.ts`                       | setFilterStats call with guard                        | VERIFIED | Lines 44-47                                                                                                                                                           |
| `src/hooks/useWaterLayers.ts`                      | Imports WATER_ATTACK_EVENT_TYPES (REV-5)              | VERIFIED | Imports from `@/lib/waterAttackEvents`; no local ATTACK_EVENT_TYPES/DESTRUCTIVE_EVENT_TYPES declarations                                                              |
| `src/components/counters/useCounterData.ts`        | Imports WATER_ATTACK_EVENT_TYPES (REV-5)              | VERIFIED | Imports from `@/lib/waterAttackEvents`; no local DESTRUCTIVE declaration                                                                                              |

### Key Link Verification

| From                                    | To                                              | Via                                         | Status   | Details                                                                                      |
| --------------------------------------- | ----------------------------------------------- | ------------------------------------------- | -------- | -------------------------------------------------------------------------------------------- |
| `server/adapters/overpass-water.ts`     | `server/types.ts`                               | WaterFacilityType import                    | VERIFIED | `import type { WaterFacility, WaterFacilityType, WaterStressIndicators } from '../types.js'` |
| `server/routes/water.ts`                | `server/cache/devFileCache.ts`                  | saveDevWaterCache/loadDevWaterCache         | VERIFIED | Both imported and called in water route                                                      |
| `src/stores/waterStore.ts`              | `src/components/ui/DevApiStatus.tsx`            | filterStats selector                        | VERIFIED | `useWaterStore((s) => s.filterStats)` in WaterFiltersSection                                 |
| `src/hooks/useWaterFetch.ts`            | `src/stores/waterStore.ts`                      | setFilterStats                              | VERIFIED | `setFilterStats(data.filterStats)` at line 46                                                |
| `src/lib/waterAttackEvents.ts`          | `src/hooks/useWaterLayers.ts`                   | `import { WATER_ATTACK_EVENT_TYPES }`       | VERIFIED | grep confirms import from `@/lib/waterAttackEvents` at line 9                                |
| `src/lib/waterAttackEvents.ts`          | `src/components/detail/WaterFacilityDetail.tsx` | same import                                 | VERIFIED | grep confirms import from `@/lib/waterAttackEvents` at line 4                                |
| `src/lib/waterAttackEvents.ts`          | `src/components/counters/useCounterData.ts`     | same import                                 | VERIFIED | grep confirms import from `@/lib/waterAttackEvents` at line 12                               |
| `server/__tests__/routes/water.test.ts` | `server/schemas/cacheResponse.ts`               | emptyStats satisfies waterFilterStatsSchema | VERIFIED | 5 required sub-fields all present in fixture; 6/6 tests pass including Zod validation path   |

### Behavioral Spot-Checks

| Behavior                                  | Command                                                                   | Result                                 | Status |
| ----------------------------------------- | ------------------------------------------------------------------------- | -------------------------------------- | ------ |
| water route test (6 tests)                | `npx vitest run server/__tests__/routes/water.test.ts`                    | 6 passed, 0 failed                     | PASS   |
| TypeScript compilation                    | `npx tsc --noEmit`                                                        | No output (clean, exit 0)              | PASS   |
| narrow attack literal removed from src/   | `grep -rn "new Set(\['airstrike', 'explosion'\])" src/`                   | 0 matches (exit 1 = no matches)        | PASS   |
| WATER_ATTACK_EVENT_TYPES consumers        | `grep -rl "WATER_ATTACK_EVENT_TYPES" src/`                                | 4 files (definition + 3 consumers)     | PASS   |
| inline ATTACK_EVENT_TYPES removed         | `grep -rn "const ATTACK_EVENT_TYPES\|const DESTRUCTIVE_EVENT_TYPES" src/` | 0 matches                              | PASS   |
| emptyStats fixture present (1 definition) | `grep -n "const emptyStats" server/__tests__/routes/water.test.ts`        | line 20 (exactly 1)                    | PASS   |
| no stats:{} literal in test file          | `grep "stats: {}" server/__tests__/routes/water.test.ts`                  | 0 matches                              | PASS   |
| treatment_plant in src/                   | `grep -rn treatment_plant src/`                                           | 0 matches                              | PASS   |
| ALL_WATER_TYPES 3 members                 | `grep ALL_WATER_TYPES src/stores/filterStore.ts`                          | `['dam', 'reservoir', 'desalination']` | PASS   |

### Requirements Coverage

| Requirement | Source Plan | Description                                                       | Status    | Evidence                                                                          |
| ----------- | ----------- | ----------------------------------------------------------------- | --------- | --------------------------------------------------------------------------------- |
| D-01        | 27.3-01     | Dam query union (waterway=dam + man_made=dam)                     | SATISFIED | overpass-water.ts:266                                                             |
| D-02        | 27.3-01     | Reservoir noise reduction via holistic filter                     | SATISFIED | isNotable \|\| (inPriority && hasName) in normalizeWaterElement                   |
| D-03        | 27.3-01/02  | treatment_plant fully removed                                     | SATISFIED | 0 matches in src/; WaterFacilityType has 3 members                                |
| D-04        | 27.3-01/02  | Dev filter diagnostics (WaterFiltersSection)                      | SATISFIED | WaterFiltersSection in DevApiStatus with full stats breakdown                     |
| D-05        | 27.3-01     | Spatial dedup 50m                                                 | SATISFIED | Dedup loop in fetchWaterFacilities                                                |
| D-06        | 27.3-01/02  | Capacity enrichment (height/volume/area)                          | SATISFIED | extractCapacityTags; WaterFacilityDetail Capacity section                         |
| D-07        | 27.3-01/02  | Population enrichment (nearest city)                              | SATISFIED | findNearestCity (51 cities, 150km); WaterFacilityDetail Population Served section |
| D-08        | 27.3-01/02  | River linkage enrichment                                          | SATISFIED | linkRiver with bbox pre-filter; WaterFacilityDetail River System section          |
| REV-1       | 27.3-01     | Holistic notability cutoff (MIN_NOTABILITY_SCORE)                 | SATISFIED | computeNotabilityScore + MIN_NOTABILITY_SCORE=25                                  |
| REV-2       | 27.3-01     | Wikidata fallback: wikidata OR (named AND priority)               | SATISFIED | normalizeWaterElement reservoir check                                             |
| REV-3       | 27.3-01     | River bbox pre-computation for O(n\*v) optimization               | SATISFIED | RIVER_BBOXES exported, bbox guard in linkRiver                                    |
| REV-4       | 27.3-01/02  | Dev file cache + filterStats forwarding                           | SATISFIED | saveDevWaterCache/loadDevWaterCache wired; useWaterFetch forwards filterStats     |
| REV-5       | 27.3-02/03  | Attacked water facilities visible on map + 3-consumer consistency | SATISFIED | WATER_ATTACK_EVENT_TYPES in shared module; map/detail/counter all agree           |

### Anti-Patterns Found

| File                          | Line     | Pattern                                                                          | Severity | Impact                                                                             |
| ----------------------------- | -------- | -------------------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------- |
| `src/hooks/useWaterLayers.ts` | ~170-179 | `console.debug` inside useMemo runs on every memo recompute, allocates Set+Array | Info     | Dev-only GC pressure; production stripped by Vite (WR-03 — pre-existing, deferred) |

No blocker or warning-level anti-patterns remain. The blocker from the previous verification (G-01 `stats: {}`) is closed. The WR-01 warning (inconsistent attack-event sets) is closed.

### Recommended Human Confirmation (non-blocking)

These items were flagged in the initial verification as requiring a dev environment with live data. The code is correctly wired. They are NOT blocking this pass verdict — they remain as quality confirmation steps for the developer before merging to main.

#### 1. Water Filters diagnostics panel in DevApiStatus

**Test:** Start dev server, hit `/api/water` fresh (bypass cache with `?refresh=true` or clear Redis). Open DevApiStatus overlay.
**Expected:** "Water Filters" section renders showing: total raw count → total kept count (percentage), per-type breakdown table (dams/reservoirs/desalination raw vs kept), rejection tally, enrichment coverage (withCapacity/withCity/withRiver), and 5-bucket notability score histogram.
**Why human:** filterStats is only populated on non-cached responses. Cannot verify rendering without live Overpass data flow.

#### 2. Enrichment sections in WaterFacilityDetail

**Test:** Click a major dam (Mosul Dam near Mosul, Tabqa Dam on Euphrates, Aswan High Dam on Nile) in the water layer.
**Expected:** Detail panel shows Capacity section (height/volume/area where OSM has tags), Population Served section (nearest city + distance), River System section (linked major river + cascading risk note). Notability Score (dev only) visible.
**Why human:** All three enrichment sections are conditional on OSM tag data. Cannot confirm render without live facility data.

#### 3. REV-5 map fix — attacked facility renders as black

**Test:** Open browser console. Look for `[useWaterLayers] attack detection:` log. If destructiveEvents > 0, pan map to a water facility in the relevant region.
**Expected:** Facility near a targeted or on_ground event renders solid black (attacked state), not stress-gradient.
**Why human:** Requires live event data. The map color change is only observable with current GDELT events near water facilities.

#### 4. WR-01 consistency confirmation — map/detail/counter agree

**Test:** Find a water facility that the map shows as solid black (attacked). Click it to open the detail panel. The Attack History section should show the same events the map used to classify it as attacked (including targeted and on_ground events, not just airstrike/explosion). The counter dropdown metric should show score 0.
**Expected:** All three views classify the facility as attacked for the same event types.
**Why human:** Requires live data with targeted/on_ground events near water facilities to confirm the three-consumer consistency is observable, not dormant.

---

_Verified: 2026-04-17T08:35:00Z_
_Verifier: Claude (gsd-verifier)_
_Re-verification: Yes — Plan 27.3-03 gap closure_
