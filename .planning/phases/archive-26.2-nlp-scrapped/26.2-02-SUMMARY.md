---
phase: 26.2-conflict-geolocation-improvement
plan: 02
subsystem: api
tags: [html-parsing, redis-caching, batch-processing, regex, title-extraction]

# Dependency graph
requires:
  - phase: 13-serverless-cache
    provides: Redis cacheGet/cacheSet utilities
provides:
  - batchFetchTitles() for batch URL title extraction with Redis caching
  - extractTitleFromHtml() for regex-based HTML title parsing
affects: [26.2-03-PLAN]

# Tech tracking
tech-stack:
  added: []
  patterns: [regex-based HTML parsing without DOM dependency, streaming body read with byte limit]

key-files:
  created:
    - server/lib/titleFetcher.ts
    - server/__tests__/lib/titleFetcher.test.ts
  modified: []

key-decisions:
  - 'Regex-based title extraction (no cheerio/jsdom dependency) -- og:title + title tag with entity decoding'
  - 'SHA-256 prefix (16 hex chars) for Redis cache keys to keep memory low'
  - 'Array.from(new Set()) instead of spread for TypeScript target compatibility'

patterns-established:
  - 'Streaming body read with MAX_BYTES cutoff: read only <head> section (10KB), cancel reader for rest'
  - 'Two-phase batch processing: cache-check all first, then fetch uncached in concurrency-limited batches'

requirements-completed: [TITLE-01, TITLE-02]

# Metrics
duration: 3min
completed: 2026-04-06
---

# Phase 26.2 Plan 02: Title Fetcher Summary

**Regex-based HTML title extraction with og:title/title-tag parsing, Redis caching (7d logical / 30d hard TTL), and concurrency-limited batch fetching (10 parallel, 5s timeout, 10KB body limit)**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-06T00:29:53Z
- **Completed:** 2026-04-06T00:33:10Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- extractTitleFromHtml parses og:title (both attribute orderings) and title tags with HTML entity decoding
- batchFetchTitles deduplicates URLs, checks Redis cache first, fetches uncached in batches of 10 with 5s timeout
- 14 unit tests covering extraction edge cases and batch behavior with mocked Redis and fetch
- No new dependencies -- uses native fetch + crypto + existing @upstash/redis

## Task Commits

Each task was committed atomically:

1. **Task 1: Create title extraction and HTML parsing utilities** (TDD)
   - `333b5fa` test: add failing tests for extractTitleFromHtml (RED)
   - `335d877` feat: implement extractTitleFromHtml with regex-based parsing (GREEN)
2. **Task 2: Create batch title fetching with Redis caching** (TDD)
   - `69a27dd` test: add batchFetchTitles tests with Redis and fetch mocks
   - `062d2ac` fix: Array.from for Set dedup (TypeScript target compat)

## Files Created/Modified

- `server/lib/titleFetcher.ts` - Title extraction (extractTitleFromHtml) and batch fetching (batchFetchTitles) with Redis caching
- `server/__tests__/lib/titleFetcher.test.ts` - 14 unit tests: 7 for extraction, 7 for batch fetching

## Decisions Made

- Regex-based HTML parsing with two og:title patterns (property-first and content-first attribute ordering) -- avoids DOM parser dependency
- SHA-256 prefix (16 hex chars) for cache keys -- short enough for Redis memory, collision-safe for URL space
- Array.from(new Set()) for deduplication instead of spread operator to avoid TypeScript downlevelIteration requirement

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] TypeScript Set spread compatibility**

- **Found during:** Task 2 (batch fetching implementation)
- **Issue:** `[...new Set(urls)]` triggers TS2802 without `--downlevelIteration` flag
- **Fix:** Changed to `Array.from(new Set(urls))`
- **Files modified:** server/lib/titleFetcher.ts
- **Verification:** `npx tsc --noEmit server/lib/titleFetcher.ts` passes (no titleFetcher errors)
- **Committed in:** `062d2ac`

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Minor TypeScript compatibility fix. No scope creep.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- titleFetcher.ts exports batchFetchTitles and extractTitleFromHtml ready for Plan 03 (NLP geo validation)
- Plan 03 will import batchFetchTitles to cross-validate GDELT event locations against article titles

## Self-Check: PASSED

All files and commits verified:

- server/lib/titleFetcher.ts: FOUND
- server/**tests**/lib/titleFetcher.test.ts: FOUND
- 333b5fa: FOUND
- 335d877: FOUND
- 69a27dd: FOUND
- 062d2ac: FOUND

---

_Phase: 26.2-conflict-geolocation-improvement_
_Completed: 2026-04-06_
