---
phase: 01-project-scaffolding-theme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/vite-env.d.ts
  - src/styles/app.css
  - src/stores/uiStore.ts
  - src/types/ui.ts
  - src/components/layout/AppShell.tsx
  - src/components/layout/TitleSlot.tsx
  - src/components/layout/CountersSlot.tsx
  - src/components/layout/LayerTogglesSlot.tsx
  - src/components/layout/FiltersSlot.tsx
  - src/components/layout/DetailPanelSlot.tsx
  - src/components/ui/OverlayPanel.tsx
  - src/test/setup.ts
  - src/__tests__/App.test.tsx
  - src/__tests__/AppShell.test.tsx
  - src/__tests__/uiStore.test.ts
  - src/__tests__/theme.test.ts
autonomous: true
requirements:
  - INFRA-02

must_haves:
  truths:
    - "Running npm run dev launches a dark-themed app in the browser with hot reload"
    - "The app displays a full-viewport dark shell with floating overlay placeholder regions"
    - "TypeScript compilation passes with strict mode (npx tsc --noEmit)"
    - "All overlay regions (title, counters, toggles, filters, detail panel) are present in the DOM"
    - "The detail panel slides in/out via Zustand store toggle"
    - "Color accent variables (blue, red, green, yellow) are defined and usable as Tailwind classes"
  artifacts:
    - path: "src/styles/app.css"
      provides: "Dark theme with Tailwind v4 @theme tokens (accent colors, surface palette, z-index scale)"
      contains: "@theme"
    - path: "src/components/layout/AppShell.tsx"
      provides: "Full-viewport shell with z-indexed overlay regions"
      exports: ["AppShell"]
      min_lines: 20
    - path: "src/stores/uiStore.ts"
      provides: "UI state for panel visibility toggles"
      exports: ["useUIStore"]
    - path: "src/components/layout/DetailPanelSlot.tsx"
      provides: "Slide-in detail panel controlled by Zustand store"
      exports: ["DetailPanelSlot"]
    - path: "src/components/ui/OverlayPanel.tsx"
      provides: "Reusable semi-transparent overlay panel component"
      exports: ["OverlayPanel"]
    - path: "vite.config.ts"
      provides: "Vite config with React and Tailwind v4 plugins plus @ path alias"
      contains: "@tailwindcss/vite"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/layout/AppShell.tsx"
      via: "default import renders AppShell"
      pattern: "import.*AppShell"
    - from: "src/components/layout/AppShell.tsx"
      to: "src/components/layout/DetailPanelSlot.tsx"
      via: "renders DetailPanelSlot within the shell"
      pattern: "<DetailPanelSlot"
    - from: "src/components/layout/DetailPanelSlot.tsx"
      to: "src/stores/uiStore.ts"
      via: "reads isDetailPanelOpen from Zustand store"
      pattern: "useUIStore"
    - from: "src/main.tsx"
      to: "src/styles/app.css"
      via: "imports the global CSS with theme tokens"
      pattern: "import.*styles/app.css"
---

<objective>
Scaffold the Iran Conflict Monitor application and build the dark-themed shell layout with all floating overlay regions.

Purpose: Establish the runnable project foundation -- build tool, type safety, CSS theme, component structure, UI state management, and test infrastructure -- so that all subsequent phases (map, data feeds, controls) can slot into the defined overlay regions.

Output: A Vite dev server serving a full-viewport dark shell with placeholder overlay regions for title/branding, analytics counters, layer toggles, filter controls, and a slide-in detail panel. All controlled by a Zustand UI store with passing tests.
</objective>

<execution_context>
@/Users/zackmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffolding-theme/01-CONTEXT.md
@.planning/phases/01-project-scaffolding-theme/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite project, install dependencies, configure build tools</name>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, index.html, src/main.tsx, src/vite-env.d.ts, src/test/setup.ts</files>
  <action>
1. Scaffold the project in the current directory using the Vite react-ts template:
   ```bash
   npm create vite@6 . -- --template react-ts
   ```
   If the directory is not empty (due to .planning/, docs/, .git), the scaffolder may prompt or fail. In that case, scaffold into a temporary directory and move files:
   ```bash
   npm create vite@6 temp-scaffold -- --template react-ts
   cp -r temp-scaffold/* temp-scaffold/.* . 2>/dev/null; rm -rf temp-scaffold
   ```
   Ensure .git and .planning directories are NOT overwritten.

2. Verify `package.json` has `"type": "module"` (Vite requires ESM). Pin TypeScript to `~5.9.3` to avoid TS 6.0 breaking changes (releases March 17).

3. Install production dependencies:
   ```bash
   npm install tailwindcss @tailwindcss/vite zustand
   ```

4. Install dev dependencies for testing:
   ```bash
   npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom
   ```

5. Update `vite.config.ts` to include the Tailwind v4 Vite plugin AND a `@/` path alias:
   ```typescript
   import { defineConfig } from 'vite';
   import react from '@vitejs/plugin-react';
   import tailwindcss from '@tailwindcss/vite';
   import path from 'path';

   export default defineConfig({
     plugins: [react(), tailwindcss()],
     resolve: {
       alias: {
         '@': path.resolve(__dirname, './src'),
       },
     },
     test: {
       environment: 'jsdom',
       setupFiles: ['./src/test/setup.ts'],
       globals: true,
     },
   });
   ```

6. Update `tsconfig.app.json` to include path alias mapping:
   Add `"baseUrl": "."` and `"paths": { "@/*": ["./src/*"] }` to compilerOptions. Ensure `"strict": true` is present. Ensure `"types": ["vitest/globals"]` is in compilerOptions for global test APIs.

7. Remove ALL Vite template demo content: delete `src/App.css`, `src/index.css`, `src/assets/react.svg`, and any demo counter code from `src/App.tsx`. Replace `src/App.tsx` with a minimal stub that just renders `<div>App</div>` for now (Task 2 will replace it).

8. Update `src/main.tsx` to import `@/styles/app.css` instead of the deleted `index.css`.

9. Create `src/test/setup.ts` that imports `@testing-library/jest-dom`:
   ```typescript
   import '@testing-library/jest-dom';
   ```

10. Create a minimal `src/styles/app.css` placeholder so the import in main.tsx resolves:
    ```css
    /* Theme tokens added in Task 2 */
    ```

11. Do NOT create `tailwind.config.js` or `postcss.config.js` -- Tailwind v4 uses CSS-first configuration via the Vite plugin.
  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && npx tsc --noEmit && npx vite build 2>&1 | tail -5 && node -e "const p = require('./package.json'); const ok = p.type === 'module' && p.dependencies?.tailwindcss && p.dependencies?.zustand && p.devDependencies?.vitest; console.log(ok ? 'PASS: all deps present' : 'FAIL: missing deps')"</automated>
  </verify>
  <done>
    - `npm run dev` starts the Vite dev server without errors
    - `npx vite build` completes without errors (confirming Vite plugin config is valid)
    - TypeScript compiles in strict mode with no errors (`npx tsc --noEmit`)
    - package.json has `"type": "module"`, TypeScript pinned to ~5.9.x
    - tailwindcss, @tailwindcss/vite, and zustand are in dependencies
    - vitest, @testing-library/react, @testing-library/jest-dom, jsdom are in devDependencies
    - No `tailwind.config.js` or `postcss.config.js` files exist
    - Vite template demo content (counter, logos, demo CSS) is fully removed
    - `@/` path alias is configured in both vite.config.ts and tsconfig
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test stubs, theme CSS, types, and Zustand store (Wave 0 + foundation)</name>
  <files>src/__tests__/uiStore.test.ts, src/__tests__/theme.test.ts, src/__tests__/AppShell.test.tsx, src/__tests__/App.test.tsx, src/styles/app.css, src/types/ui.ts, src/stores/uiStore.ts</files>
  <behavior>
    - uiStore.test.ts: Initial state has all panels closed/collapsed; openDetailPanel sets isDetailPanelOpen to true; closeDetailPanel sets isDetailPanelOpen to false; toggleCounters flips isCountersCollapsed; toggleFilters flips isFiltersExpanded
    - theme.test.ts: CSS file contains @theme directive; CSS defines accent-blue, accent-red, accent-green, accent-yellow colors; CSS defines surface colors; CSS defines z-index scale variables; CSS defines detail panel width variable
    - AppShell.test.tsx: Shell renders map container region; Shell renders title slot in top-left; Shell renders counters slot in top-left; Shell renders layer toggles slot in top-right; Shell renders filters slot in top-right; Shell renders detail panel slot
    - App.test.tsx: App renders without crashing; App renders the AppShell component
  </behavior>
  <action>
This task establishes ALL test files (Nyquist Wave 0) and implements the non-component foundation (theme, types, store). Component tests will be RED at the end of this task -- that is expected. Store and theme tests will be GREEN.

**Wave 0: Create ALL test stubs**

1. Create `src/__tests__/uiStore.test.ts` -- test the Zustand store directly:
   - Import useUIStore from `@/stores/uiStore`
   - Test initial state: all booleans false (isDetailPanelOpen, isCountersCollapsed, isFiltersExpanded)
   - Test openDetailPanel: sets isDetailPanelOpen to true
   - Test closeDetailPanel: sets isDetailPanelOpen to false
   - Test toggleCounters: flips isCountersCollapsed
   - Test toggleFilters: flips isFiltersExpanded
   - Use `useUIStore.getState()` and `useUIStore.setState()` for direct store testing without React rendering

2. Create `src/__tests__/theme.test.ts` -- test that CSS theme tokens exist by reading the file:
   - Use `fs.readFileSync` to read `src/styles/app.css`
   - Assert it contains `@theme`, all four accent colors (`accent-blue`, `accent-red`, `accent-green`, `accent-yellow`), surface colors, z-index scale, and `--width-detail-panel`

3. Create `src/__tests__/AppShell.test.tsx` -- test the layout has all required regions:
   - Render `<AppShell />` with testing-library
   - Assert presence of data-testid attributes: `map-container`, `title-slot`, `counters-slot`, `layer-toggles-slot`, `filters-slot`, `detail-panel-slot`

4. Create `src/__tests__/App.test.tsx` -- smoke test:
   - Render `<App />`, assert it does not throw
   - Assert the AppShell is in the document (check for data-testid="map-container" as a proxy)

**Foundation implementation (makes store + theme tests GREEN):**

5. Create `src/styles/app.css` with the Tailwind v4 CSS-first dark theme. Use the exact pattern from RESEARCH.md Pattern 1:
   - `@import "tailwindcss";`
   - `@theme { }` block with all semantic color tokens:
     - Accent colors: `--color-accent-blue` (oklch blue), `--color-accent-red` (oklch red), `--color-accent-green` (oklch green), `--color-accent-yellow` (oklch yellow)
     - Surface colors: `--color-surface` (near-black ~oklch 0.145), `--color-surface-elevated` (slightly lighter ~oklch 0.185), `--color-surface-overlay` (with 85% opacity)
     - Text colors: `--color-text-primary` (white-ish), `--color-text-secondary` (muted gray), `--color-text-muted` (dim gray)
     - Border colors: `--color-border` (white/10%), `--color-border-accent` (white/20%)
     - Z-index scale: `--z-map: 0`, `--z-overlay: 10`, `--z-panel: 20`, `--z-controls: 30`, `--z-modal: 40`
     - Layout: `--width-detail-panel: 320px`
   - `@layer base { }` block setting html background to surface, body margin 0, system font stack
   - Do NOT create tailwind.config.js. Do NOT use PostCSS config.

6. Create `src/types/ui.ts` with the UIState interface:
   ```typescript
   export interface UIState {
     isDetailPanelOpen: boolean;
     isCountersCollapsed: boolean;
     isFiltersExpanded: boolean;
     openDetailPanel: () => void;
     closeDetailPanel: () => void;
     toggleCounters: () => void;
     toggleFilters: () => void;
   }
   ```

7. Create `src/stores/uiStore.ts` using the curried `create<UIState>()()` pattern (per Zustand 5 / RESEARCH.md Pattern 3). Import UIState from `@/types/ui`. Initial state: all booleans false. Implement all four actions.

**Verify partial progress:**

Run `npx vitest run --reporter=verbose`. Expected results:
- uiStore.test.ts -- ALL PASS (store is implemented)
- theme.test.ts -- ALL PASS (CSS file has all tokens)
- AppShell.test.tsx -- ALL FAIL (component not yet created, this is expected RED)
- App.test.tsx -- ALL FAIL (AppShell not yet wired, this is expected RED)

Commit with message: `test(01-01): add test stubs and foundation (store + theme GREEN, components RED)`
  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && npx vitest run src/__tests__/uiStore.test.ts src/__tests__/theme.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - All 4 test files exist: App.test.tsx, AppShell.test.tsx, uiStore.test.ts, theme.test.ts
    - uiStore.test.ts passes (store logic verified)
    - theme.test.ts passes (CSS tokens verified)
    - AppShell.test.tsx and App.test.tsx exist but fail (expected RED -- components not yet implemented)
    - src/styles/app.css has @theme block with all accent, surface, text, border, z-index, and layout tokens
    - src/types/ui.ts exports UIState interface
    - src/stores/uiStore.ts exports useUIStore with all actions implemented
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement layout components and wire AppShell (GREEN phase)</name>
  <files>src/components/ui/OverlayPanel.tsx, src/components/layout/AppShell.tsx, src/components/layout/TitleSlot.tsx, src/components/layout/CountersSlot.tsx, src/components/layout/LayerTogglesSlot.tsx, src/components/layout/FiltersSlot.tsx, src/components/layout/DetailPanelSlot.tsx, src/App.tsx</files>
  <action>
Implement all layout components to make the remaining RED tests pass (GREEN phase). Tests already exist from Task 2 -- do NOT modify test files.

1. Create `src/components/ui/OverlayPanel.tsx` -- a reusable semi-transparent dark panel component (per RESEARCH.md Pattern 4):
   - Props: `children: React.ReactNode`, `className?: string`
   - Render a div with: `rounded-lg border border-border bg-surface-overlay px-4 py-3 shadow-lg backdrop-blur-sm`
   - Merge className prop

2. Create placeholder slot components (each uses OverlayPanel and has a data-testid):
   - `src/components/layout/TitleSlot.tsx` -- renders "IRAN CONFLICT MONITOR" title text in uppercase tracking-wider, small font, text-text-secondary. data-testid="title-slot"
   - `src/components/layout/CountersSlot.tsx` -- renders a placeholder OverlayPanel with "Counters" label. Uses useUIStore to read isCountersCollapsed. data-testid="counters-slot"
   - `src/components/layout/LayerTogglesSlot.tsx` -- renders a placeholder OverlayPanel with "Layers" label. data-testid="layer-toggles-slot"
   - `src/components/layout/FiltersSlot.tsx` -- renders a placeholder OverlayPanel with "Filters" label. Uses useUIStore to read isFiltersExpanded. data-testid="filters-slot"
   - `src/components/layout/DetailPanelSlot.tsx` -- slide-in panel per RESEARCH.md Pattern (uses transform translate, transition, reads isDetailPanelOpen from useUIStore). data-testid="detail-panel-slot". Width: var(--width-detail-panel). Includes close button that calls closeDetailPanel.

3. Create `src/components/layout/AppShell.tsx` (per RESEARCH.md Pattern 2):
   - Full viewport: `relative h-screen w-screen overflow-hidden bg-surface`
   - Map container: `absolute inset-0` with z-[var(--z-map)] and data-testid="map-container" -- renders a dark placeholder div (Phase 2 replaces with actual map)
   - Top-left group: `absolute top-4 left-4` with z-[var(--z-overlay)], flex column, contains TitleSlot + CountersSlot
   - Top-right group: `absolute top-4 right-4` with z-[var(--z-controls)], flex column items-end, contains LayerTogglesSlot + FiltersSlot
   - DetailPanelSlot positioned absolutely by itself (it manages its own positioning)

4. Update `src/App.tsx` to import and render `<AppShell />`.

5. Run all tests -- they should now ALL pass (GREEN).
  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && npx vitest run --reporter=verbose && npx tsc --noEmit && npx vite build 2>&1 | tail -3</automated>
  </verify>
  <done>
    - All tests pass: App.test.tsx, AppShell.test.tsx, uiStore.test.ts, theme.test.ts
    - TypeScript compiles in strict mode with zero errors
    - Vite build completes successfully
    - `npm run dev` shows a full-viewport near-black shell with floating overlay panels
    - All six overlay regions visible in DOM with correct data-testid attributes
    - Accent colors (blue, red, green, yellow) defined as Tailwind theme tokens and usable as classes (e.g., `text-accent-blue`)
    - Detail panel slides in/out when store state changes
    - Counters slot responds to collapse toggle
    - No tailwind.config.js or postcss.config.js exists
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- TypeScript strict compilation passes
2. `npx vitest run --reporter=verbose` -- All unit tests pass
3. `npx vite build` -- Production build completes without errors
4. `npm run dev` -- Dev server starts, browser shows dark shell with overlay regions
5. Inspect DOM in browser dev tools: all data-testid regions present (map-container, title-slot, counters-slot, layer-toggles-slot, filters-slot, detail-panel-slot)
6. Verify no `tailwind.config.js` or `postcss.config.js` files exist
7. Verify `src/styles/app.css` uses `@theme` directive (Tailwind v4 CSS-first config)
</verification>

<success_criteria>
- Running `npm run dev` launches a dark-themed intelligence dashboard shell in the browser
- The shell has a full-viewport dark background with floating HUD-style overlay placeholders
- TypeScript strict mode compilation passes (`npx tsc --noEmit` returns 0)
- All Vitest tests pass (store toggles, layout regions, theme tokens, app smoke test)
- Tailwind v4 CSS-first theme defines all semantic color tokens (accent-blue/red/green/yellow, surface, text)
- Project structure has clean directories: components/layout, components/ui, hooks, stores, api, types, styles
- The shell is ready to receive a Deck.gl + MapLibre map in the center viewport region (Phase 2)
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffolding-theme/01-01-SUMMARY.md`
</output>
