---
status: partial
phase: 27.3.1-water-facility-retry-and-cleanup
source: [27.3.1-VERIFICATION.md]
started: 2026-04-19T23:55:00Z
updated: 2026-04-19T23:55:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. DevApiStatus modal open/close, tab switching, Escape priority, backdrop behavior

expected: |
Open the running dev app. Click the dev-only "API ~/!" badge in the Topbar
right cluster (between ResetButton and NotificationBell). The DevApiStatus
modal opens centered at `z-index: var(--z-modal)`; backdrop darkens the rest
of the viewport; Overview tab active by default.

- Click **Water** tab: `WaterFiltersSection` shows populated `byCountry` rows
  (Iran, Iraq, Saudi Arabia, Egypt, UAE, Afghanistan — **NO Turkey row**).
- Click **Sites** tab: `SitesFiltersSection` shows `byType` populated
  (airbase=284, port=232, oil=99, naval=60, nuclear=45).
- Press **Escape**: the modal closes FIRST — before any nav-stack pop or
  detail-panel close.
- Click the **backdrop**: modal closes.
- Click inside the modal container: modal stays open.
- Modal body scrolls with `max-h-[85vh]` + `overflow-y-auto` when Water/Sites
  tabs render populated tables (no viewport overflow).
  result: [pending]

### 2. Redis envelope round-trip — live Upstash filterStats persistence

expected: |
With the dev server running against a real Upstash Redis instance:

1. Delete Redis keys: `water:facilities:v2` and `sites:v3`.
2. Curl `/api/water` and `/api/sites` once each:
   - Response: `filterStats.source === "snapshot"`
   - Response: `filterStats.rejections.no_name > 0`
   - Response: `filterStats.rejections.excluded_turkey > 0`
   - Response: `filterStats.byCountry.Iran` has non-zero type bucket
3. Curl `/api/water` and `/api/sites` a second time each:
   - Response: `filterStats.source === "redis"` (override applied)
   - Response: `filterStats.rejections.*` **identical** to first-hit values
   - Response: `byCountry` / `byType` tallies **identical** to first-hit values

This proves the `{facilities, filterStats}` and `{sites, filterStats}`
envelopes round-trip through Upstash without losing observability data.
result: [pending]

### 3. No raw generic labels in water facility detail panels

expected: |
With the map rendering water facilities, hover or click any facility whose
OSM name is non-Latin script only (Persian / Arabic / Georgian). Detail
panel displays a derived label like:

- "Dam on Tigris" (river fallback)
- "Dam near Mosul" (city fallback)
- "Dam at 36.12°N, 43.00°E" (coord fallback)

Confirm **no** facility shows a raw "Dam" / "Reservoir" / "Desalination
Plant" label. The 137 admitted facilities whose `label` field in
`src/data/water-facilities.json` equals `"Dam"` / `"Reservoir"` /
`"Desalination Plant"` must flow through client-side
`getWaterFacilityDisplayName` in `src/lib/waterLabel.ts` and render
sentinel-rewritten text.
result: [pending]

## Summary

total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps
