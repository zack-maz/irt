---
phase: 02-base-map
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/map/BaseMap.tsx
  - src/components/map/MapLoadingScreen.tsx
  - src/components/map/MapVignette.tsx
  - src/components/map/CoordinateReadout.tsx
  - src/components/map/CompassControl.tsx
  - src/components/map/constants.ts
  - src/components/layout/AppShell.tsx
  - src/styles/app.css
  - src/__tests__/BaseMap.test.tsx
  - src/__tests__/CoordinateReadout.test.tsx
  - src/__tests__/MapLoadingScreen.test.tsx
autonomous: false
requirements: [MAP-01]

must_haves:
  truths:
    - "User sees a 2.5D dark map centered on Iran with visible terrain bumps"
    - "User can pan, zoom, rotate, and tilt the map smoothly"
    - "Map fills the full viewport inside the dark-themed AppShell"
    - "Road name labels are hidden; country/city labels remain visible"
    - "Country borders are visually emphasized (brighter/thicker than default)"
    - "Water bodies have a subtle dark blue tint"
    - "A compass indicator is visible; double-clicking it resets the view"
    - "Cursor position shows as lat/lon readout in bottom-right"
    - "A scale bar is visible in bottom-right"
    - "A dark vignette frames the viewport edges"
    - "A loading screen with pulse animation appears and fades out when map is ready"
  artifacts:
    - path: "src/components/map/BaseMap.tsx"
      provides: "Main 2.5D map component with deck.gl overlay, terrain, style customization"
      min_lines: 80
      exports: ["BaseMap"]
    - path: "src/components/map/MapLoadingScreen.tsx"
      provides: "Loading overlay that fades out on map load"
      exports: ["MapLoadingScreen"]
    - path: "src/components/map/MapVignette.tsx"
      provides: "CSS vignette overlay for scope effect"
      exports: ["MapVignette"]
    - path: "src/components/map/CoordinateReadout.tsx"
      provides: "Lat/lon display updating on cursor move"
      exports: ["CoordinateReadout"]
    - path: "src/components/map/CompassControl.tsx"
      provides: "Custom compass with double-click reset behavior"
      exports: ["CompassControl"]
    - path: "src/components/map/constants.ts"
      provides: "Map configuration constants (view state, bounds, style URL, layer IDs)"
      exports: ["INITIAL_VIEW_STATE", "MAX_BOUNDS", "MAP_STYLE", "TERRAIN_CONFIG"]
    - path: "src/components/layout/AppShell.tsx"
      provides: "Updated shell injecting BaseMap into map-container"
  key_links:
    - from: "src/components/map/BaseMap.tsx"
      to: "src/components/map/DeckGLOverlay.tsx"
      via: "DeckGLOverlay rendered as child of Map"
      pattern: "<DeckGLOverlay"
    - from: "src/components/map/BaseMap.tsx"
      to: "src/stores/mapStore.ts"
      via: "setMapLoaded in onLoad, setCursorPosition in onMouseMove"
      pattern: "useMapStore"
    - from: "src/components/map/CoordinateReadout.tsx"
      to: "src/stores/mapStore.ts"
      via: "reads cursorLng, cursorLat from store"
      pattern: "useMapStore"
    - from: "src/components/layout/AppShell.tsx"
      to: "src/components/map/BaseMap.tsx"
      via: "BaseMap rendered inside map-container div"
      pattern: "<BaseMap"
    - from: "src/components/map/BaseMap.tsx"
      to: "CARTO Dark Matter style.json"
      via: "mapStyle prop URL"
      pattern: "basemaps.cartocdn.com"
---

<objective>
Build the complete interactive 2.5D map of Iran with all overlay UI (vignette, loading screen, coordinate readout, compass, scale bar), wire it into the AppShell, and visually verify the result.

Purpose: Deliver the MAP-01 requirement -- an interactive 2.5D dark map of Iran with pan, zoom, rotate, terrain, and HUD-style overlay elements.

Output: BaseMap and 4 overlay components, updated AppShell, passing tests, visually verified map.
</objective>

<execution_context>
@/Users/zackmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-base-map/02-RESEARCH.md
@.planning/phases/02-base-map/02-CONTEXT.md
@.planning/phases/02-base-map/02-VALIDATION.md
@.planning/phases/02-base-map/02-01-SUMMARY.md

<interfaces>
<!-- Contracts created by Plan 01 that this plan builds against -->

From src/stores/mapStore.ts (created in Plan 01):
```typescript
interface MapState {
  isMapLoaded: boolean;
  cursorLng: number;
  cursorLat: number;
  setMapLoaded: () => void;
  setCursorPosition: (lng: number, lat: number) => void;
}
export const useMapStore = create<MapState>()((set) => ({ ... }));
```

From src/components/map/DeckGLOverlay.tsx (created in Plan 01):
```typescript
export function DeckGLOverlay(props: MapboxOverlayProps): null;
// Usage: <DeckGLOverlay layers={[]} />
```

From src/components/layout/AppShell.tsx (to be modified):
```typescript
// Replace this placeholder inside data-testid="map-container":
<div className="h-full w-full bg-surface" />
// With: <BaseMap />
```

From src/components/ui/OverlayPanel.tsx:
```typescript
export function OverlayPanel({ children, className }: { children: React.ReactNode; className?: string }): JSX.Element;
```

From src/styles/app.css:
```css
--z-map: 0;
--z-overlay: 10;
--z-panel: 20;
--z-controls: 30;
--color-surface: oklch(0.145 0 0);
--color-text-secondary: oklch(0.7 0 0);
--color-text-muted: oklch(0.5 0 0);
--color-border: oklch(1 0 0 / 10%);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create map constants, BaseMap, and all overlay components</name>
  <files>src/components/map/constants.ts, src/components/map/BaseMap.tsx, src/components/map/MapLoadingScreen.tsx, src/components/map/MapVignette.tsx, src/components/map/CoordinateReadout.tsx, src/components/map/CompassControl.tsx, src/styles/app.css</files>
  <action>
1. Create `src/components/map/constants.ts` with module-level constants to prevent re-render issues:
   ```typescript
   export const INITIAL_VIEW_STATE = {
     longitude: 53.7,
     latitude: 32.4,
     zoom: 5.5,
     pitch: 35,
     bearing: 0,
   };
   export const MAX_BOUNDS: [number, number, number, number] = [30, 15, 70, 45]; // [west, south, east, north]
   export const MAP_STYLE = 'https://basemaps.cartocdn.com/gl/dark-matter-gl-style/style.json';
   export const TERRAIN_SOURCE_URL = 'https://demotiles.maplibre.org/terrain-tiles/tiles.json';
   export const TERRAIN_CONFIG = { source: 'terrain-dem', exaggeration: 1.5 };
   // CARTO Dark Matter layer IDs for style customization
   export const ROAD_LABEL_LAYERS = ['roadname_minor', 'roadname_sec', 'roadname_pri', 'roadname_major'];
   export const BORDER_LAYERS = ['boundary_country_outline', 'boundary_country_inner'];
   export const WATER_LAYERS = ['water', 'water_shadow', 'waterway'];
   export const MINOR_FEATURE_LAYERS = ['place_suburbs', 'place_hamlet', 'poi'];
   ```

2. Create `src/components/map/MapLoadingScreen.tsx`:
   - Accept `isLoaded: boolean` prop
   - Render an absolute-positioned div at z-[var(--z-overlay)] with bg-surface
   - When not loaded: opacity-100, show a small (h-3 w-3 or h-4 w-4) rounded-full element with animate-pulse using bg-text-muted
   - When loaded: opacity-0 with pointer-events-none
   - Use `transition-opacity duration-500` for the ~300-500ms fade-in per user decision
   - Export named: `MapLoadingScreen`

3. Create `src/components/map/MapVignette.tsx`:
   - Render a pointer-events-none absolute inset-0 div at z-[var(--z-overlay)]
   - Use `background: radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.6) 100%)` via inline style
   - Export named: `MapVignette`

4. Create `src/components/map/CoordinateReadout.tsx`:
   - Read cursorLng and cursorLat from useMapStore
   - Render inside an OverlayPanel with `className="text-xs font-mono text-text-secondary"`
   - Format: `{lat.toFixed(4)}N, {lng.toFixed(4)}E` (handle negative values -- show S/W as appropriate for the Middle East region, though in practice values will be positive in this bounds)
   - Export named: `CoordinateReadout`

5. Create `src/components/map/CompassControl.tsx`:
   - This is a wrapper that uses `useMap` hook from `@vis.gl/react-maplibre` to access the map instance
   - On mount, find the NavigationControl compass button in the DOM (`.maplibregl-ctrl-compass`) and attach a `dblclick` event listener
   - On double-click, call `map.flyTo({ center: [53.7, 32.4], zoom: 5.5, pitch: 35, bearing: 0, duration: 1000 })` using values from constants
   - Clean up the event listener on unmount
   - Render null (behavior-only component)
   - Export named: `CompassControl`

6. Create `src/components/map/BaseMap.tsx`:
   - Import Map, Source, Layer, NavigationControl, ScaleControl from `@vis.gl/react-maplibre`
   - Import `maplibre-gl/dist/maplibre-gl.css` (critical -- without this, controls and canvas break)
   - Import DeckGLOverlay from `./DeckGLOverlay`
   - Import useMapStore from `@/stores/mapStore`
   - Import all constants from `./constants`
   - Import MapLoadingScreen, MapVignette, CoordinateReadout, CompassControl
   - Use `const { isMapLoaded, setMapLoaded, setCursorPosition } = useMapStore()` (extract only what's needed)
   - Create `handleLoad` callback (useCallback, empty deps):
     * Get map from `e.target`
     * Hide road labels: iterate ROAD_LABEL_LAYERS, check `map.getLayer(id)`, call `map.setLayoutProperty(id, 'visibility', 'none')`
     * Hide minor features: iterate MINOR_FEATURE_LAYERS, same hide pattern
     * Brighten borders: iterate BORDER_LAYERS, check getLayer, set `line-color` to `#888888`, `line-width` to 1.5
     * Tint water: iterate WATER_LAYERS, check getLayer, set `fill-color` to `#0a1628` (for fill layers) -- note `waterway` is a line layer so set `line-color` to `#0a1628` for that one
     * Call `setMapLoaded()`
   - Create `handleMouseMove` callback (useCallback, empty deps):
     * Extract `e.lngLat.lng` and `e.lngLat.lat`
     * Call `setCursorPosition(lng, lat)`
   - Render a relative container div (h-full w-full):
     * `<MapLoadingScreen isLoaded={isMapLoaded} />`
     * `<MapVignette />`
     * `<Map>` with:
       - `initialViewState={INITIAL_VIEW_STATE}`
       - `style={{ width: '100%', height: '100%' }}`
       - `mapStyle={MAP_STYLE}`
       - `maxBounds={MAX_BOUNDS}`
       - `minZoom={3}`, `maxZoom={15}`, `maxPitch={60}`
       - `terrain={TERRAIN_CONFIG}`
       - `onLoad={handleLoad}`
       - `onMouseMove={handleMouseMove}`
     * Inside Map children:
       - `<Source id="terrain-dem" type="raster-dem" url={TERRAIN_SOURCE_URL} tileSize={256} />`
       - `<Layer id="terrain-hillshade" type="hillshade" source="terrain-dem" paint={{ 'hillshade-exaggeration': 0.3, 'hillshade-shadow-color': '#000000', 'hillshade-highlight-color': '#222222' }} />`
       - `<NavigationControl showZoom={false} showCompass={true} visualizePitch={true} position="bottom-right" />`
       - `<ScaleControl unit="metric" position="bottom-right" />`
       - `<DeckGLOverlay layers={[]} />`
       - `<CompassControl />`
     * Below the Map (sibling), in bottom-right position:
       - `<div className="absolute bottom-8 right-14 z-[var(--z-controls)]">` containing `<CoordinateReadout />`
       - Position this above the MapLibre attribution and to the left of the scale/compass controls. Exact position may need tuning -- start with bottom-8 right-14 and adjust.
   - Export named: `BaseMap`

7. Update `src/styles/app.css`:
   - Add `@import "maplibre-gl/dist/maplibre-gl.css";` BEFORE the `@import "tailwindcss"` line (MapLibre CSS must load so Tailwind can override if needed). Actually, it may be cleaner to import the CSS directly in BaseMap.tsx. If importing in app.css causes build issues (Tailwind v4 may not resolve node_modules CSS via @import), just import it in BaseMap.tsx as `import 'maplibre-gl/dist/maplibre-gl.css'` and skip changing app.css.
   - If any custom CSS is needed for the vignette or loading animation that goes beyond Tailwind utilities, add it here. But prefer Tailwind utilities and inline styles.

IMPORTANT per research anti-patterns:
- Do NOT use DeckGL component as root with Map as child (reverse-controlled mode). Use overlaid mode via MapboxOverlay/useControl.
- Do NOT create inline objects for Map props. All constants are in constants.ts.
- Do NOT fetch/modify CARTO style.json before passing to Map. Customize imperatively in onLoad.
- Always guard setLayoutProperty/setPaintProperty with getLayer() checks.
  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && npx tsc --noEmit 2>&1 | head -20 && npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <done>BaseMap.tsx renders Map with CARTO dark tiles, terrain, deck.gl overlay, and all overlay components. MapLoadingScreen, MapVignette, CoordinateReadout, CompassControl all exist and export correctly. Constants are defined in constants.ts. TypeScript compiles. Existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire BaseMap into AppShell and activate test stubs</name>
  <files>src/components/layout/AppShell.tsx, src/__tests__/BaseMap.test.tsx, src/__tests__/CoordinateReadout.test.tsx, src/__tests__/MapLoadingScreen.test.tsx</files>
  <action>
1. Update `src/components/layout/AppShell.tsx`:
   - Import `BaseMap` from `@/components/map/BaseMap`
   - Replace the placeholder `<div className="h-full w-full bg-surface" />` inside the `data-testid="map-container"` div with `<BaseMap />`
   - Keep everything else the same (all overlay slots remain)

2. Activate test stubs in `src/__tests__/BaseMap.test.tsx` (remove skip/todo, write real tests):
   - Test "renders map container": Render `<BaseMap />`, assert the component produces output (check for the relative container div)
   - Test "hides road labels on load": Mock the maplibre-gl Map's getLayer to return truthy for road label layer IDs. Simulate the onLoad callback. Assert setLayoutProperty was called with each road label layer ID, 'visibility', 'none'.
   - Use the mocks from Plan 01 (src/test/__mocks__/maplibre-gl.ts and deck-gl-mapbox.ts). Also mock `@vis.gl/react-maplibre` -- mock Map to render children and call onLoad with a fake map object, mock Source/Layer/NavigationControl/ScaleControl to render null, mock useControl to return { setProps: vi.fn() }, mock useMap to return { current: fakeMapInstance }.

3. Activate test stubs in `src/__tests__/CoordinateReadout.test.tsx`:
   - Mock useMapStore to return specific cursorLng/cursorLat values (e.g., 53.7000, 32.4000)
   - Render `<CoordinateReadout />`
   - Assert text contains "32.4000" and "53.7000"

4. Activate test stubs in `src/__tests__/MapLoadingScreen.test.tsx`:
   - Test "shows loading state": Render `<MapLoadingScreen isLoaded={false} />`, assert container has `opacity-100` class
   - Test "fades out when loaded": Render `<MapLoadingScreen isLoaded={true} />`, assert container has `opacity-0` class and `pointer-events-none`
  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && npx vitest run 2>&1 | tail -30</automated>
  </verify>
  <done>AppShell renders BaseMap instead of placeholder div. All Phase 2 tests pass: BaseMap renders (MAP-01a), road labels hidden (MAP-01d), DeckGLOverlay wired (MAP-01b), mapStore works (MAP-01c), CoordinateReadout displays coords (MAP-01e), MapLoadingScreen fades (MAP-01f). All Phase 1 tests still pass. `npx vitest run` exits with 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of interactive 2.5D map</name>
  <action>Human verifies the complete map implementation visually in a browser.</action>
  <what-built>Complete interactive 2.5D map of Iran with CARTO Dark Matter tiles, 3D terrain, deck.gl overlay, style customization (hidden road labels, bright borders, blue-tinted water), compass control with reset, coordinate readout, scale bar, vignette effect, and loading screen with fade-in.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app in a browser
    2. Verify the map is centered on Iran with a slight 2.5D tilt and visible terrain bumps on mountain ranges (Zagros, Alborz)
    3. Verify the base style is dark (CARTO Dark Matter) with country and city labels visible
    4. Verify road name labels are NOT visible
    5. Verify country borders are brighter/thicker than typical dark map borders
    6. Verify water bodies (Persian Gulf, Caspian Sea) have a subtle dark blue tint
    7. Pan the map by clicking and dragging -- smooth movement, bounded to the Middle East region
    8. Zoom in/out with scroll wheel -- min ~3, max ~15
    9. Right-click drag to rotate the map bearing
    10. Ctrl+click+drag to adjust pitch/tilt
    11. Verify compass is visible in bottom-right area, no zoom +/- buttons
    12. Double-click the compass -- map should animate back to the default centered-on-Iran view
    13. Move cursor over the map -- lat/lon readout updates in bottom-right
    14. Verify scale bar is visible in bottom-right area
    15. Verify a subtle dark vignette effect frames the viewport edges
    16. Reload the page -- observe brief loading pulse animation, then map fades in smoothly
    17. Verify all existing UI overlays (title, counters, layer toggles, filters) still float above the map
  </how-to-verify>
  <verify>Visual inspection in browser per steps above</verify>
  <done>All 17 verification steps pass. Map renders correctly with all overlay UI intact.</done>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run` -- all tests pass (Phase 1 + Phase 2)
- `npx tsc --noEmit` -- TypeScript compiles without errors
- `npm run build` -- production build succeeds
- Visual: Map renders centered on Iran with terrain, dark tiles, styled borders/water, compass, scale, coordinate readout, vignette, loading fade-in
</verification>

<success_criteria>
- 2.5D map renders centered on Iran (~32.4N, 53.7E) at zoom ~5.5 with ~35 degree pitch
- CARTO Dark Matter tiles load with dark aesthetic
- 3D terrain visible on mountain ranges (low exaggeration)
- Road labels hidden, country/city labels visible, borders brightened, water tinted
- Pan, zoom, rotate, tilt all work with MapLibre defaults
- Compass visible (no zoom buttons), double-click resets to default view
- Coordinate readout updates on cursor move in bottom-right
- Scale bar visible in bottom-right
- Vignette effect visible around viewport edges
- Loading screen with pulse animation fades out when map loads
- Map fills the AppShell viewport behind all overlay panels
- All automated tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-base-map/02-02-SUMMARY.md`
</output>
