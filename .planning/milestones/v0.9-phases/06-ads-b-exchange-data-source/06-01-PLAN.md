---
phase: 06-ads-b-exchange-data-source
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/adapters/adsb-exchange.ts
  - server/routes/flights.ts
  - server/config.ts
  - server/constants.ts
  - server/types.ts
  - server/__tests__/adapters/adsb-exchange.test.ts
  - server/__tests__/routes/flights.test.ts
  - server/__tests__/security.test.ts
autonomous: true
requirements: [DATA-04]

must_haves:
  truths:
    - 'ADS-B Exchange adapter fetches aircraft from RapidAPI and normalizes to FlightEntity[]'
    - 'Flight route dispatches to correct adapter based on ?source= query param'
    - 'Rate limited (429) responses are distinguished from generic errors'
    - 'Stale cache is served when rate limited, with rateLimited flag in response'
    - 'ADS-B Exchange API key is not leaked in any API response'
  artifacts:
    - path: 'server/adapters/adsb-exchange.ts'
      provides: 'ADS-B Exchange V2 adapter with unit conversion and normalization'
      exports: ['fetchFlights']
    - path: 'server/routes/flights.ts'
      provides: 'Source-dispatching flight route with per-source caching'
      exports: ['flightsRouter']
    - path: 'server/types.ts'
      provides: 'FlightSource type and RateLimitError class'
      contains: 'FlightSource'
    - path: 'server/__tests__/adapters/adsb-exchange.test.ts'
      provides: 'Adapter unit tests for normalization, ground filter, unit conversion'
    - path: 'server/__tests__/routes/flights.test.ts'
      provides: 'Route dispatch tests for source param, rate limit handling, stale cache'
  key_links:
    - from: 'server/routes/flights.ts'
      to: 'server/adapters/adsb-exchange.ts'
      via: 'conditional import dispatch based on source param'
      pattern: "source === 'adsb' \\? fetchAdsbExchange : fetchOpenSky"
    - from: 'server/adapters/adsb-exchange.ts'
      to: 'RapidAPI'
      via: 'fetch with X-RapidAPI-Key header'
      pattern: 'X-RapidAPI-Key'
    - from: 'server/routes/flights.ts'
      to: 'server/types.ts'
      via: 'RateLimitError instanceof check'
      pattern: 'RateLimitError'
---

<objective>
Server-side ADS-B Exchange integration: adapter, route dispatch, and rate limit handling.

Purpose: Enable the Express server to fetch flight data from ADS-B Exchange as an alternative to OpenSky, with proper per-source caching and rate limit distinction.
Output: Working `/api/flights?source=adsb` endpoint that returns normalized FlightEntity[] data from ADS-B Exchange V2 API, with all server tests passing.
</objective>

<execution_context>
@/Users/zackmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ads-b-exchange-data-source/06-CONTEXT.md
@.planning/phases/06-ads-b-exchange-data-source/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From server/types.ts:

```typescript
export interface FlightEntity extends MapEntityBase {
  type: 'flight';
  data: {
    icao24: string;
    callsign: string;
    originCountry: string;
    velocity: number | null;
    heading: number | null;
    altitude: number | null;
    onGround: boolean;
    verticalRate: number | null;
    unidentified: boolean;
  };
}

export interface BoundingBox {
  south: number;
  north: number;
  west: number;
  east: number;
}

export interface CacheResponse<T> {
  data: T;
  stale: boolean;
  lastFresh: number;
}
```

From server/adapters/opensky.ts (pattern to follow):

```typescript
export async function fetchFlights(bbox: BoundingBox): Promise<FlightEntity[]>;
```

From server/cache/entityCache.ts:

```typescript
export class EntityCache<T> {
  constructor(ttlMs: number);
  get(): CacheResponse<T> | null;
  set(data: T): void;
  clear(): void;
}
```

From server/constants.ts:

```typescript
export const IRAN_BBOX: BoundingBox = { south: 25.0, north: 40.0, west: 44.0, east: 63.5 };
export const CACHE_TTL = { flights: 10_000, ships: 0, events: 300_000 } as const;
```

From server/config.ts:

```typescript
export interface AppConfig {
  port: number;
  corsOrigin: string;
  opensky: { clientId: string; clientSecret: string };
  aisstream: { apiKey: string };
  acled: { email: string; password: string };
}
export const config: AppConfig; // Lazy proxy
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ADS-B Exchange adapter with types and constants</name>
  <files>
    server/types.ts,
    server/constants.ts,
    server/adapters/adsb-exchange.ts,
    server/__tests__/adapters/adsb-exchange.test.ts
  </files>
  <behavior>
    - Test: normalizeAircraft converts a valid V2 aircraft object to FlightEntity with correct unit conversions (knots->m/s via *0.514444, feet->meters via *0.3048, ft/min->m/s via *0.00508)
    - Test: normalizeAircraft returns null when lat or lon is missing (null/undefined)
    - Test: normalizeAircraft returns null when alt_baro is the string "ground" (ground traffic filter)
    - Test: normalizeAircraft sets unidentified=true when flight field is empty or whitespace-only
    - Test: normalizeAircraft strips tilde prefix from hex when constructing entity ID (e.g., "~abc123" -> "flight-abc123")
    - Test: normalizeAircraft sets originCountry to empty string (ADS-B Exchange does not provide it)
    - Test: normalizeAircraft handles null/undefined optional fields (gs, track, baro_rate) by setting them to null
    - Test: fetchFlights calls RapidAPI endpoint with correct URL pattern and headers, throws RateLimitError on 429
  </behavior>
  <action>
    1. Add to `server/types.ts`:
       - `export type FlightSource = 'opensky' | 'adsb';`
       - `export class RateLimitError extends Error { constructor(message: string) { super(message); this.name = 'RateLimitError'; } }`
       Note: Use explicit field + constructor assignment (no parameter properties) per erasableSyntaxOnly convention.

    2. Add to `server/constants.ts`:
       - `IRAN_CENTER: { lat: 32.5, lon: 53.75 }` -- center point for ADS-B Exchange radius query
       - `ADSB_RADIUS_NM: 250` -- max radius for V2 API
       - `ADSB_POLL_INTERVAL: 260_000` -- 260 seconds, sustainable for 10K requests/month
       - Add `adsbFlights: 260_000` to `CACHE_TTL` (same as poll interval -- ADS-B data is fresh for one poll cycle)
       - Unit conversion constants: `KNOTS_TO_MS = 0.514444`, `FEET_TO_METERS = 0.3048`, `FPM_TO_MS = 0.00508`

    3. Create `server/adapters/adsb-exchange.ts`:
       - Define `AdsbAircraft` interface matching V2 response fields: `hex`, `flight?`, `lat?`, `lon?`, `alt_baro?: number | 'ground'`, `gs?`, `track?`, `baro_rate?`, `r?`, `dbFlags?`
       - Define `AdsbResponse` interface: `{ ac: AdsbAircraft[] | null, msg: string, now: number, total: number }`
       - `RAPIDAPI_HOST = 'adsbexchange-com1.p.rapidapi.com'`
       - `normalizeAircraft(ac: AdsbAircraft): FlightEntity | null` -- filter null lat/lon, filter alt_baro==="ground", strip tilde from hex for ID, trim flight for callsign, convert units, set originCountry='', derive unidentified flag
       - `export async function fetchFlights(): Promise<FlightEntity[]>` -- reads API key from `process.env.ADSB_EXCHANGE_API_KEY` (inline, not via config.ts -- follows the lazy credential pattern). Uses `IRAN_CENTER` and `ADSB_RADIUS_NM` from constants. Sends `X-RapidAPI-Key` and `X-RapidAPI-Host` headers. Throws `RateLimitError` on 429. Returns normalized, filtered FlightEntity[].
       - Note: This adapter does NOT take a BoundingBox param (unlike OpenSky). It uses the fixed center+radius. The route layer handles the dispatch.

    4. Create `server/__tests__/adapters/adsb-exchange.test.ts`:
       - Use `@vitest-environment node` directive
       - Mock `global.fetch` for API tests
       - Mock `process.env.ADSB_EXCHANGE_API_KEY` for credential tests
       - Test all behaviors listed above
       - Follow existing opensky.test.ts patterns for structure

  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && npx vitest run server/__tests__/adapters/adsb-exchange.test.ts -x</automated>
  </verify>
  <done>
    ADS-B Exchange adapter normalizes V2 aircraft data to FlightEntity with correct unit conversions. Ground traffic filtered. Tilde-prefixed hex handled. RateLimitError thrown on 429. All adapter tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flight route dispatch with per-source caching and rate limit handling</name>
  <files>
    server/routes/flights.ts,
    server/__tests__/routes/flights.test.ts,
    server/__tests__/security.test.ts
  </files>
  <behavior>
    - Test: GET /api/flights (no source param) dispatches to OpenSky adapter (default)
    - Test: GET /api/flights?source=opensky dispatches to OpenSky adapter
    - Test: GET /api/flights?source=adsb dispatches to ADS-B Exchange adapter
    - Test: GET /api/flights?source=invalid falls back to OpenSky (default)
    - Test: Separate caches per source -- OpenSky cache does not serve ADS-B requests and vice versa
    - Test: When ADS-B adapter throws RateLimitError and cache exists, response includes { rateLimited: true } with stale cache data
    - Test: When ADS-B adapter throws RateLimitError and no cache exists, returns 429 with { error: 'Rate limited', rateLimited: true }
    - Test: When ADS-B source is requested but ADSB_EXCHANGE_API_KEY env var is not set, returns 503 with descriptive error
    - Test: ADS-B Exchange API key does not appear in any /api/flights?source=adsb response
  </behavior>
  <action>
    1. Refactor `server/routes/flights.ts`:
       - Import `fetchFlights as fetchAdsbExchange` from `'../adapters/adsb-exchange.js'`
       - Import `RateLimitError` and `FlightSource` from `'../types.js'`
       - Create two cache instances: `openskyCache = new EntityCache<FlightEntity[]>(CACHE_TTL.flights)` and `adsbCache = new EntityCache<FlightEntity[]>(CACHE_TTL.adsbFlights)`
       - Route handler reads `req.query.source` -- if `'adsb'` use ADS-B adapter+cache, otherwise default to OpenSky adapter+cache
       - For ADS-B source: check `process.env.ADSB_EXCHANGE_API_KEY` exists, if not return `res.status(503).json({ error: 'ADS-B Exchange API key not configured' })`
       - In catch block: if `err instanceof RateLimitError`, serve stale cache with `{ ...cached, rateLimited: true }` or return 429 with `{ error: 'Rate limited', rateLimited: true }` if no cache
       - Keep existing OpenSky error handling (serve stale or re-throw)

    2. Create `server/__tests__/routes/flights.test.ts`:
       - Use `@vitest-environment node` directive
       - Mock both adapter modules (`../adapters/opensky.js` and `../adapters/adsb-exchange.js`)
       - Mock config module (same pattern as security.test.ts)
       - Set `process.env.ADSB_EXCHANGE_API_KEY = 'test-key'` for tests that need it
       - Use `createApp()` + `server.listen(0)` pattern from security.test.ts for integration-style route tests
       - Test all behaviors listed above

    3. Update `server/__tests__/security.test.ts`:
       - Add mock for `'../adapters/adsb-exchange.js'` returning test FlightEntity[] data
       - Add `process.env.ADSB_EXCHANGE_API_KEY = 'SECRET_ADSB_API_KEY'` to mock config
       - Add test: `GET /api/flights?source=adsb response does not contain ADS-B Exchange API key` -- verify response text does not contain 'SECRET_ADSB_API_KEY'

  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && npx vitest run server/__tests__/routes/flights.test.ts server/__tests__/security.test.ts -x</automated>
  </verify>
  <done>
    Flight route correctly dispatches between OpenSky and ADS-B Exchange based on source query param. Per-source caching prevents cross-contamination. Rate limit errors return rateLimited flag with stale data. Missing API key returns 503. No credential leaks. All route and security tests pass.
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/zackmaz/Desktop/my_world && npx vitest run server/ -x
```
All server tests pass, including new adapter tests, route dispatch tests, and updated security tests.
</verification>

<success_criteria>

- `GET /api/flights` returns OpenSky data (backward compatible)
- `GET /api/flights?source=adsb` returns ADS-B Exchange data normalized to FlightEntity
- `GET /api/flights?source=adsb` returns 503 when API key missing
- Rate limit (429) returns stale cache with `rateLimited: true` flag
- No API keys leaked in responses
- All server tests pass: `npx vitest run server/`
  </success_criteria>

<output>
After completion, create `.planning/phases/06-ads-b-exchange-data-source/06-01-SUMMARY.md`
</output>
