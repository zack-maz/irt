---
phase: 26.3-production-code-cleanup
plan: 04
subsystem: api
tags: [express, error-handling, compression, middleware, graceful-shutdown]

# Dependency graph
requires:
  - phase: 26.3-02
    provides: pino logger with req.log and req.id
  - phase: 26.3-03
    provides: consolidated config with env.NODE_ENV
provides:
  - AppError class for typed error throwing across routes
  - Centralized error handler with consistent { error, code, statusCode, requestId } envelope
  - Response compression middleware for local dev
  - Graceful SIGTERM shutdown handler
affects: [all server routes, future API routes, vercel deployment]

# Tech tracking
tech-stack:
  added: [compression, '@types/compression']
  patterns: [AppError throw pattern, consistent error JSON envelope, VERCEL-gated middleware]

key-files:
  created:
    - server/__tests__/middleware/errorHandler.test.ts
  modified:
    - server/middleware/errorHandler.ts
    - server/index.ts
    - server/vercel-entry.ts
    - server/routes/flights.ts
    - server/routes/ships.ts
    - server/routes/markets.ts
    - server/middleware/rateLimit.ts

key-decisions:
  - 'AppError uses explicit property assignment (not parameter properties) due to erasableSyntaxOnly tsconfig'
  - 'Compression gated by !process.env.VERCEL to avoid double-compression with Vercel CDN edge handling'
  - 'Graceful shutdown only in isMainModule block -- Vercel has its own 500ms SIGTERM window'

patterns-established:
  - 'AppError(statusCode, code, message) for typed route errors'
  - 'Consistent error envelope: { error, code, statusCode, requestId }'
  - 'Stack traces in dev, stripped in production'
  - 'VERCEL env check for middleware that should only run locally'

requirements-completed: [CLN-04, CLN-09]

# Metrics
duration: 4min
completed: 2026-04-06
---

# Phase 26.3 Plan 04: Error Handling, Compression & Shutdown Summary

**AppError class with consistent { error, code, statusCode, requestId } JSON envelope, gzip compression for local dev, and SIGTERM graceful shutdown**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-06T22:55:32Z
- **Completed:** 2026-04-06T22:59:41Z
- **Tasks:** 2 (Task 1 TDD)
- **Files modified:** 8

## Accomplishments

- AppError class for typed error throwing with statusCode and machine-readable code
- Centralized errorHandler producing consistent JSON envelope with request ID tracing
- All inline error responses across 4 routes + rateLimit middleware updated to consistent shape
- Response compression middleware gated for non-Vercel environments
- Graceful SIGTERM shutdown handler with 10s forced exit timeout

## Task Commits

Each task was committed atomically:

1. **Task 1 (RED): Add failing tests for error handler** - `d5db693` (test)
2. **Task 1 (GREEN): Upgrade error handler with AppError and consistent shape** - `935d807` (feat)
3. **Task 2: Add compression middleware and graceful shutdown** - `3dbda18` (feat)

## Files Created/Modified

- `server/middleware/errorHandler.ts` - AppError class + centralized error handler with consistent JSON shape
- `server/__tests__/middleware/errorHandler.test.ts` - 7 tests covering AppError, JSON shape, stack traces, logging
- `server/index.ts` - Compression middleware + SIGTERM graceful shutdown handler
- `server/vercel-entry.ts` - Documentation comment about Vercel SIGTERM handling
- `server/routes/flights.ts` - Updated 503 and 429 inline errors to include code/statusCode
- `server/routes/ships.ts` - Updated 500 inline error to include code/statusCode
- `server/routes/markets.ts` - Updated 502 inline error to include code/statusCode
- `server/middleware/rateLimit.ts` - Updated 429 response to include code/statusCode

## Decisions Made

- AppError uses explicit property assignment instead of parameter properties (erasableSyntaxOnly in tsconfig.server.json)
- Compression middleware gated by `!process.env.VERCEL` -- Vercel CDN handles gzip/brotli at the edge, local dev gets compression for realistic testing
- Graceful shutdown handler only in isMainModule block -- Vercel has its own SIGTERM handling (500ms window) and Upstash Redis is REST-based (no connections to drain)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed AppError parameter property syntax**

- **Found during:** Task 2 (type checking)
- **Issue:** `public readonly` parameter properties are not allowed with `erasableSyntaxOnly` in tsconfig.server.json
- **Fix:** Converted to explicit property declarations with assignment in constructor body
- **Files modified:** server/middleware/errorHandler.ts
- **Verification:** `npx tsc --noEmit -p tsconfig.server.json` exits 0
- **Committed in:** 3dbda18 (Task 2 commit)

**2. [Rule 1 - Bug] Updated rateLimit test for new error shape**

- **Found during:** Task 1 (server test suite)
- **Issue:** Existing rateLimit.test.ts expected old `{ error: 'Too many requests' }` shape without code/statusCode
- **Fix:** Updated expectation to include `code: 'RATE_LIMITED', statusCode: 429`
- **Files modified:** server/**tests**/rateLimit.test.ts
- **Verification:** All 478 server tests pass
- **Committed in:** 935d807 (Task 1 commit)

---

**Total deviations:** 2 auto-fixed (2 bugs)
**Impact on plan:** Both auto-fixes necessary for correctness. No scope creep.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All server middleware upgraded: error handling, compression, shutdown
- AppError class ready for use in future routes/adapters
- Consistent error shape established for API consumers

## Self-Check: PASSED

All 8 modified/created files verified on disk. All 3 task commits verified in git log. All 478 server tests pass. tsc strict-mode type check exits 0.

---

_Phase: 26.3-production-code-cleanup_
_Completed: 2026-04-06_
