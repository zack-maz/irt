---
phase: 26.3-production-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/lib/nlpGeoValidator.ts
  - server/lib/titleFetcher.ts
  - server/lib/nlpExtractor.ts
  - server/lib/geoValidation.ts
  - server/lib/eventScoring.ts
  - server/lib/eventAudit.ts
  - server/lib/dispersion.ts
  - server/adapters/gdelt.ts
  - server/adapters/overpass-water.ts
  - server/config.ts
  - server/__tests__/lib/nlpGeoValidator.test.ts
  - server/__tests__/lib/titleFetcher.test.ts
  - src/data/me-cities.json
  - scripts/extract-geonames.ts
autonomous: true
requirements: [CLN-01, CLN-13]

must_haves:
  truths:
    - 'All Phase 26.2 NLP files are deleted from the codebase'
    - 'Reverted files contain only pre-26.2 code (no Phase C additions)'
    - 'Server TypeScript compiles without unused-import or unused-variable errors'
    - 'All existing tests pass (no regressions from reversion)'
  artifacts:
    - path: 'server/lib/nlpGeoValidator.ts'
      provides: 'Deleted (must not exist)'
    - path: 'server/lib/titleFetcher.ts'
      provides: 'Deleted (must not exist)'
    - path: 'server/__tests__/lib/nlpGeoValidator.test.ts'
      provides: 'Deleted (must not exist)'
    - path: 'server/__tests__/lib/titleFetcher.test.ts'
      provides: 'Deleted (must not exist)'
    - path: 'src/data/me-cities.json'
      provides: 'Deleted (must not exist)'
    - path: 'scripts/extract-geonames.ts'
      provides: 'Deleted (must not exist)'
  key_links:
    - from: 'server/adapters/gdelt.ts'
      to: 'server/lib/eventScoring.ts'
      via: 'import'
      pattern: 'import.*scoreEvent.*eventScoring'
    - from: 'server/adapters/gdelt.ts'
      to: 'server/lib/geoValidation.ts'
      via: 'import'
      pattern: 'import.*geoValidation'
---

<objective>
Remove all Phase 26.2 NLP dead code and clean up unused imports/variables across the server codebase.

Purpose: Unblock clean TypeScript compilation as the foundation for all subsequent cleanup work. Phase 26.2 was scrapped -- its NLP pipeline code (nlpGeoValidator, titleFetcher, me-cities lexicon, extract-geonames script) must be fully removed, and files it modified (gdelt.ts, geoValidation.ts, eventScoring.ts, eventAudit.ts, config.ts) must be surgically reverted to pre-26.2 state while preserving Phase 22/22.1 changes.
Output: Clean-compiling server with zero dead code from Phase 26.2.
</objective>

<execution_context>
@/Users/zackmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.3-production-code-cleanup/26.3-CONTEXT.md
@.planning/phases/26.3-production-code-cleanup/26.3-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Phase 26.2 NLP files and revert modified files</name>
  <files>
    server/lib/nlpGeoValidator.ts,
    server/lib/titleFetcher.ts,
    server/__tests__/lib/nlpGeoValidator.test.ts,
    server/__tests__/lib/titleFetcher.test.ts,
    src/data/me-cities.json,
    scripts/extract-geonames.ts,
    server/lib/nlpExtractor.ts,
    server/adapters/gdelt.ts,
    server/lib/geoValidation.ts,
    server/lib/eventScoring.ts,
    server/lib/eventAudit.ts,
    server/config.ts
  </files>
  <action>
    CRITICAL: Use `git log --oneline` to find the last commit BEFORE Phase 26.2 work began (look for commits before any 26.2 prefixed messages). Use `git diff {pre-26.2-commit}..HEAD -- {file}` for each modified file to identify ONLY 26.2-specific changes. Do NOT use file-level `git checkout` -- that would revert Phase 22/22.1 changes too.

    **Files to DELETE entirely (git rm):**
    - server/lib/nlpGeoValidator.ts
    - server/lib/titleFetcher.ts
    - server/__tests__/lib/nlpGeoValidator.test.ts
    - server/__tests__/lib/titleFetcher.test.ts
    - src/data/me-cities.json
    - scripts/extract-geonames.ts

    **Files to SURGICALLY REVERT (remove only 26.2 additions):**

    1. `server/lib/nlpExtractor.ts` -- Remove `extractActorsAndPlaces()`, `lookupCityCoords()`, `ME_LEXICON` additions, and any imports added for those (me-cities.json import, compromise additions). Keep `extractTriple()` and `ArticleTriple` type intact (used by relevanceScorer.ts).

    2. `server/adapters/gdelt.ts` -- Remove the entire "Phase C" validation block (batchFetchTitles import, sanitizeLocation function, actor-name check logic, the Phase C NLP cross-validation section in parseAndFilter). Revert parseAndFilter back to synchronous if it was made async for Phase C. Remove unused `PipelineTrace` import and `EXCLUDED_BASE_CODES` variable that are flagged by tsc. Keep all Phase 22/22.1 code (dispersion wiring, Bellingcat corroboration, pipeline trace fields from Phase 22).

    3. `server/lib/geoValidation.ts` -- Remove expanded CITY_CENTROIDS entries (revert to original 42 entries). Remove WE/GZ FIPS code additions from 26.2. Keep all pre-26.2 FIPS tables and validation logic.

    4. `server/lib/eventScoring.ts` -- Remove CAMEO exclusion changes from 26.2. Keep the pre-26.2 exclusion list (180, 182, 190, 192 were already there from Phase 22).

    5. `server/lib/eventAudit.ts` -- Remove PhaseCChecks additions (any fields like titleFetched, nlpActors, nlpPlaces, validationStatus). Keep the pre-26.2 audit trace structure.

    6. `server/config.ts` -- Remove threshold changes from 26.2. Keep all pre-26.2 config values and structure intact.

    **After reversions, add TODO comments** to hardcoded tables:
    - In geoValidation.ts near CITY_CENTROIDS: `// TODO(26.2): Replace hardcoded 42-entry table with GeoNames-sourced data`
    - In geoValidation.ts near FIPS tables: `// TODO(26.2): Externalize FIPS lookup tables`
    - In eventScoring.ts near CAMEO exclusions: `// TODO(26.2): Review CAMEO exclusion list in GDELT redo`

  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && npx tsc --noEmit -p tsconfig.server.json 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <done>All 6 NLP files deleted. 6 modified files surgically reverted to pre-26.2 state. Hardcoded tables flagged with TODO comments. Server compiles with zero tsc errors.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up remaining unused imports and variables across server</name>
  <files>
    server/lib/dispersion.ts,
    server/adapters/overpass-water.ts
  </files>
  <action>
    Fix remaining tsc warnings in the server codebase that were visible before this plan:

    1. `server/lib/dispersion.ts` -- Remove unused `detectCentroid` import (line 5 flagged by tsc TS6133).

    2. `server/adapters/overpass-water.ts` -- Remove unused `CENTROIDS` import (line 18 flagged by tsc TS6133).

    3. Run `npx tsc --noEmit -p tsconfig.server.json` and fix any remaining unused import/variable warnings. The goal is zero server-side tsc errors.

    4. Run the full test suite to confirm no regressions from Task 1 reversions: `npx vitest run server/`. If any tests fail due to imports from deleted files, update those test files to remove the dead imports.

  </action>
  <verify>
    <automated>cd /Users/zackmaz/Desktop/my_world && npx tsc --noEmit -p tsconfig.server.json && npx vitest run server/ --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>Server TypeScript compiles with zero errors. All server tests pass. No unused imports or variables remain in server/ directory.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p tsconfig.server.json` exits 0 (zero errors)
2. `npx vitest run server/` all pass (no regressions)
3. `ls server/lib/nlpGeoValidator.ts server/lib/titleFetcher.ts 2>&1` shows "No such file"
4. `grep -r "nlpGeoValidator\|titleFetcher\|batchFetchTitles\|me-cities" server/ --include="*.ts"` returns no results
</verification>

<success_criteria>

- Zero Phase 26.2 NLP files exist in the codebase
- Server compiles cleanly with zero tsc errors
- All server tests pass without regression
- Hardcoded tables flagged with TODO comments for Phase 26.2 redo
  </success_criteria>

<output>
After completion, create `.planning/phases/26.3-production-code-cleanup/26.3-01-SUMMARY.md`
</output>
