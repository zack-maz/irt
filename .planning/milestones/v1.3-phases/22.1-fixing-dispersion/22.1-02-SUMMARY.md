---
phase: 22.1-fixing-dispersion
plan: 02
subsystem: ui
tags: [react, zustand, useRef, fly-to, search, map]

# Dependency graph
requires:
  - phase: 19-search-filter
    provides: useQuerySync hook with near: tag handling
provides:
  - Ref-based fly-to deduplication guard in useQuerySync
  - Tests for fly-to dedup behavior (4 new tests)
affects: [search, map-camera, near-queries]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Ref-based dedup guard for store side-effects triggered by useEffect"
    - "Zustand subscribe() for tracking store method calls in tests"

key-files:
  created: []
  modified:
    - src/hooks/useQuerySync.ts
    - src/hooks/useQuerySync.test.ts

key-decisions:
  - "Used simple lat/lng !== equality for pin comparison (coordinates from lookup table, exact match correct)"
  - "Added else-if branch to reset ref when near: tag absent from query (not just when explicitly null)"

patterns-established:
  - "lastFlownPinRef pattern: track last coordinates to deduplicate map fly-to actions"

requirements-completed: [FIX-04, FIX-05]

# Metrics
duration: 7min
completed: 2026-04-02
---

# Phase 22.1 Plan 02: Fly-To Deduplication Summary

**Ref-guarded camera fly-to in useQuerySync prevents re-centering when editing non-near: search terms**

## Performance

- **Duration:** 7 min
- **Started:** 2026-04-02T01:51:42Z
- **Completed:** 2026-04-02T01:58:56Z
- **Tasks:** 1 (TDD: RED + GREEN)
- **Files modified:** 2

## Accomplishments

- Camera now flies to near: location exactly once per unique pin coordinate change
- Editing other search terms (e.g., adding `type:airstrike`) while `near:Tehran` is active no longer re-flies the camera
- Clearing near: and re-entering same location correctly triggers fly-to again
- 4 new tests covering all dedup scenarios (35 total in file, all passing)

## Task Commits

Each task was committed atomically:

1. **Task 1 (RED): Add failing tests for fly-to dedup** - `9770284` (test)
2. **Task 1 (GREEN): Implement ref-based fly-to guard** - `ee0c253` (feat)

## Files Created/Modified

- `src/hooks/useQuerySync.ts` - Added `lastFlownPinRef` and `pinChanged` guard around `setFlyToTarget` call; added else-if branch to reset ref when near: tag is removed from query
- `src/hooks/useQuerySync.test.ts` - Added 4 tests in "fly-to deduplication" describe block using renderHook and Zustand subscribe() pattern

## Decisions Made

- Used simple `!==` on lat/lng numbers for pin comparison rather than deep equality or JSON.stringify -- pin coordinates come from the same GEO_NAMES lookup table so exact equality is correct
- Added an `else if (lastFlownPinRef.current !== null)` branch to handle the case where near: tag is completely removed from query (not just set to null) -- deriveFiltersFromAST returns undefined (not null) for proximityPin when no near: tags exist, so the explicit null branch alone was insufficient

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Ref not resetting when near: tag removed from query**

- **Found during:** Task 1 (GREEN phase)
- **Issue:** `deriveFiltersFromAST` returns `undefined` (not `null`) for `proximityPin` when no near: tags exist. The plan's code only had `else { lastFlownPinRef.current = null }` inside the `if (filterUpdates.proximityPin !== undefined)` block, which never fires when near: tag is absent.
- **Fix:** Added `else if (lastFlownPinRef.current !== null) { lastFlownPinRef.current = null }` after the main block to reset the ref when no near: tag exists in the current query.
- **Files modified:** src/hooks/useQuerySync.ts
- **Verification:** "resets fly-to state when near: is cleared" test passes
- **Committed in:** ee0c253

---

**Total deviations:** 1 auto-fixed (1 bug fix)
**Impact on plan:** Essential for correct clear-and-re-fly behavior. No scope creep.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Fly-to dedup fix complete and tested
- Plan 22.1-01 (dispersion pipeline relocation) still needs execution
- No blockers for remaining phase work

## Self-Check: PASSED

- [x] src/hooks/useQuerySync.ts exists
- [x] src/hooks/useQuerySync.test.ts exists
- [x] SUMMARY.md exists
- [x] Commit 9770284 (test) exists
- [x] Commit ee0c253 (feat) exists

---

_Phase: 22.1-fixing-dispersion_
_Completed: 2026-04-02_
