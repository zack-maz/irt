---
phase: 27-quick
plan: jf3
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/counters/useCounterData.ts
  - src/components/layout/CountersSlot.tsx
  - src/components/layout/Sidebar.tsx
  - src/__tests__/useCounterData.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - 'Events section shows 5 counter rows: Airstrikes, Ground Combat, Explosions, Targeted, Other'
    - 'Each counter row is independently gated by its corresponding filterStore sub-toggle'
    - 'Counter dropdown entity lists show entities matching only their specific type group'
    - 'Existing flight, ship, site, and water counters are unchanged'
  artifacts:
    - path: 'src/components/counters/useCounterData.ts'
      provides: '5 event count fields + 5 entity arrays in CounterValues/CounterEntities'
    - path: 'src/components/layout/Sidebar.tsx'
      provides: 'CountersContent with 5 event CounterRows'
    - path: 'src/components/layout/CountersSlot.tsx'
      provides: 'Deprecated CountersSlot with 5 event CounterRows (kept for backward compat)'
    - path: 'src/__tests__/useCounterData.test.ts'
      provides: 'Updated test expectations for 5-type event counters'
  key_links:
    - from: 'src/components/counters/useCounterData.ts'
      to: 'src/types/ui.ts'
      via: 'CONFLICT_TOGGLE_GROUPS import'
      pattern: "CONFLICT_TOGGLE_GROUPS\\.show"
    - from: 'src/components/counters/useCounterData.ts'
      to: 'src/stores/filterStore.ts'
      via: '5 sub-toggle selectors'
      pattern: "s\\.show(Airstrikes|OnGround|Explosions|Targeted|Other)"
---

<objective>
Update the events counters from 3 rows (Airstrikes, Ground Combat, Targeted) to 5 rows matching the new Phase 27 attack-vector ontology (Airstrikes, Ground Combat, Explosions, Targeted, Other).

Purpose: The old counter system lumped on_ground + explosion + other into a single "Ground Combat" bucket. The new 5-type ontology has independent toggle state for each type, so counters must reflect that 1:1.

Output: Updated useCounterData hook with 5 event counts/entity arrays, updated Sidebar CountersContent and CountersSlot with 5 event rows, updated tests.
</objective>

<execution_context>
@/Users/zackmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zackmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/counters/useCounterData.ts
@src/components/layout/CountersSlot.tsx
@src/components/layout/Sidebar.tsx
@src/__tests__/useCounterData.test.ts
@src/types/ui.ts
@src/stores/filterStore.ts

<interfaces>
<!-- Current CONFLICT_TOGGLE_GROUPS (already updated to 5-type ontology in ui.ts) -->
From src/types/ui.ts:
```typescript
export const CONFLICT_TOGGLE_GROUPS = {
  showAirstrikes: ['airstrike'] as const,
  showOnGround: ['on_ground'] as const,
  showExplosions: ['explosion'] as const,
  showTargeted: ['targeted'] as const,
  showOther: ['other'] as const,
} as const;

export const EVENT_TYPE_LABELS: Record<string, string> = {
airstrike: 'Airstrike',
on_ground: 'Ground Combat',
explosion: 'Explosion',
targeted: 'Targeted',
other: 'Other',
};

````

From src/stores/filterStore.ts (visibility toggles — already 5 sub-toggles):
```typescript
showEvents: boolean;       // Master toggle for all conflict events
showAirstrikes: boolean;
showOnGround: boolean;
showExplosions: boolean;
showTargeted: boolean;
showOther: boolean;
````

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update useCounterData hook — split 3 event buckets into 5</name>
  <files>src/components/counters/useCounterData.ts</files>
  <action>
  Modify `useCounterData.ts` to replace the 3-bucket event counting with 5 independent buckets:

1. **Replace type group constants** (lines 68-74):
   - Remove `GROUND_COMBAT_TYPES` (the old aggregate of showOnGround + showExplosions + showOther)
   - Keep `AIRSTRIKE_TYPES` as-is: `CONFLICT_TOGGLE_GROUPS.showAirstrikes`
   - Add `ON_GROUND_TYPES`: `CONFLICT_TOGGLE_GROUPS.showOnGround`
   - Add `EXPLOSION_TYPES`: `CONFLICT_TOGGLE_GROUPS.showExplosions`
   - Keep `TARGETED_TYPES` as-is: `CONFLICT_TOGGLE_GROUPS.showTargeted`
   - Add `OTHER_TYPES`: `CONFLICT_TOGGLE_GROUPS.showOther`

2. **Update `CounterValues` interface** (lines 39-47):
   - Replace `groundCombat: number` with three fields: `onGround: number`, `explosions: number`, `other: number`
   - Keep `airstrikes` and `targeted` as-is

3. **Update `CounterEntities` interface** (lines 58-66):
   - Replace `groundCombatEvents: CounterEntity[]` with three fields: `onGroundEvents: CounterEntity[]`, `explosionEvents: CounterEntity[]`, `otherEvents: CounterEntity[]`
   - Keep `airstrikeEvents` and `targetedEvents` as-is

4. **Update counting logic inside useMemo** (around lines 220-225):
   - Remove the composite `showGroundCombatToggle` derivation
   - Replace `groundCombatCount` with three independent counts:
     - `onGroundCount = showEvents && showOnGroundToggle ? countByGroup(severityFilteredEvents, ON_GROUND_TYPES) : 0`
     - `explosionsCount = showEvents && showExplosionsToggle ? countByGroup(severityFilteredEvents, EXPLOSION_TYPES) : 0`
     - `otherCount = showEvents && showOtherToggle ? countByGroup(severityFilteredEvents, OTHER_TYPES) : 0`

5. **Update entity arrays** (around lines 329-343):
   - Replace `groundCombatEventEntities` with three arrays:
     - `onGroundEventEntities` gated by `showEvents && showOnGroundToggle`
     - `explosionEventEntities` gated by `showEvents && showExplosionsToggle`
     - `otherEventEntities` gated by `showEvents && showOtherToggle`
   - Each uses `filterByGroup` with its respective type constant, sorted by proximity to TEHRAN/TEL_AVIV

6. **Update return object** (around lines 345-364): - Return `onGround: onGroundCount`, `explosions: explosionsCount`, `other: otherCount` instead of `groundCombat: groundCombatCount` - Return `onGroundEvents`, `explosionEvents`, `otherEvents` instead of `groundCombatEvents` in the entities record
   </action>
   <verify>
   <automated>cd /Users/zackmaz/Desktop/my_world && npx tsc --noEmit 2>&1 | head -30</automated>
   </verify>
   <done>useCounterData exports 5 independent event counts (airstrikes, onGround, explosions, targeted, other) and 5 entity arrays, each gated by its own filterStore toggle. No compile errors.</done>
   </task>

<task type="auto">
  <name>Task 2: Update Sidebar CountersContent + CountersSlot — 5 event rows</name>
  <files>src/components/layout/Sidebar.tsx, src/components/layout/CountersSlot.tsx</files>
  <action>
  Update both components that render event counter rows to show 5 rows instead of 3.

**In Sidebar.tsx `CountersContent` function (lines 137-168):**
Replace the 3 event CounterRow elements with 5:

```
Airstrikes  -> counters.airstrikes / counters.entities.airstrikeEvents  (expandedKey='airstrikes')
Ground      -> counters.onGround  / counters.entities.onGroundEvents    (expandedKey='onGround')
Explosions  -> counters.explosions / counters.entities.explosionEvents   (expandedKey='explosions')
Targeted    -> counters.targeted  / counters.entities.targetedEvents     (expandedKey='targeted')
Other       -> counters.other     / counters.entities.otherEvents        (expandedKey='other')
```

Use label "Ground" (not "Ground Combat") to keep it concise alongside the other short labels.

**In CountersSlot.tsx (lines 90-118):**
Same change — replace 3 event CounterRow elements with the same 5 rows using identical labels and expandedKey values.

Both components already import `useCounterData` and `CounterRow` — no new imports needed. The CounterRow component itself needs zero changes (it's generic).
</action>
<verify>
<automated>cd /Users/zackmaz/Desktop/my_world && npx tsc --noEmit 2>&1 | head -30</automated>
</verify>
<done>Both Sidebar CountersContent and CountersSlot render 5 event counter rows (Airstrikes, Ground, Explosions, Targeted, Other) with correct data bindings and expand keys. No compile errors.</done>
</task>

<task type="auto">
  <name>Task 3: Update useCounterData tests for 5-type ontology</name>
  <files>src/__tests__/useCounterData.test.ts</files>
  <action>
  Update test expectations in `useCounterData.test.ts` to match the new 5-field interface:

1. **'returns zero counts with empty stores'** (line 130-136):
   - Replace `expect(result.current.groundCombat).toBe(0)` with:
     - `expect(result.current.onGround).toBe(0)`
     - `expect(result.current.explosions).toBe(0)`
     - `expect(result.current.other).toBe(0)`

2. **'computes event counts matching CONFLICT_TOGGLE_GROUPS'** (line 161-177):
   - Keep `expect(result.current.airstrikes).toBe(2)` (unchanged)
   - Replace `expect(result.current.groundCombat).toBe(3)` with:
     - `expect(result.current.onGround).toBe(1)` (just on_ground)
     - `expect(result.current.explosions).toBe(1)` (just explosion)
     - `expect(result.current.other).toBe(1)` (just other)
   - Keep `expect(result.current.targeted).toBe(1)` (unchanged)

3. **'returns entities object alongside counts'** (line 192-201):
   - Replace `expect(result.current.entities.groundCombatEvents).toEqual([])` with:
     - `expect(result.current.entities.onGroundEvents).toEqual([])`
     - `expect(result.current.entities.explosionEvents).toEqual([])`
     - `expect(result.current.entities.otherEvents).toEqual([])`

4. **'entities.groundCombatEvents contains events filtered by GROUND_COMBAT_TYPES'** test (line 241-253): - Rename to 'entities split on_ground, explosion, other into separate arrays' - Use same test data (on_ground, explosion, other, airstrike) - Assert `onGroundEvents` has length 1, `explosionEvents` has length 1, `otherEvents` has length 1
   </action>
   <verify>
   <automated>cd /Users/zackmaz/Desktop/my_world && npx vitest run src/**tests**/useCounterData.test.ts 2>&1 | tail -20</automated>
   </verify>
   <done>All useCounterData tests pass with updated expectations for 5 independent event count fields and 5 entity arrays. No test references the old `groundCombat` or `groundCombatEvents` fields.</done>
   </task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero errors (type-checks the full project)
2. `npx vitest run src/__tests__/useCounterData.test.ts` — all tests pass
3. `npx vitest run` — full test suite passes (no regressions)
</verification>

<success_criteria>

- Events section in counters shows 5 rows: Airstrikes, Ground, Explosions, Targeted, Other
- Each row independently gated by its own filterStore sub-toggle (plus master showEvents gate)
- Counter dropdown entity lists are split per-type (no more lumping on_ground+explosion+other)
- All existing counter functionality (flights, ships, sites, water) unchanged
- TypeScript compiles, all tests pass
  </success_criteria>

<output>
After completion, create `.planning/quick/260409-jf3-update-events-counters-to-reflect-our-ne/260409-jf3-SUMMARY.md`
</output>
